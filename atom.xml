<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zouxiulong.github.io</id>
    <title>浮梦   的博客</title>
    <updated>2021-03-24T03:23:39.903Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zouxiulong.github.io"/>
    <link rel="self" href="https://zouxiulong.github.io/atom.xml"/>
    <subtitle>咸鱼一枚~</subtitle>
    <logo>https://zouxiulong.github.io/images/avatar.png</logo>
    <icon>https://zouxiulong.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 浮梦   的博客</rights>
    <entry>
        <title type="html"><![CDATA[js算法：快速排序]]></title>
        <id>https://zouxiulong.github.io/post/js-suan-fa-kuai-pai</id>
        <link href="https://zouxiulong.github.io/post/js-suan-fa-kuai-pai">
        </link>
        <updated>2021-03-23T09:44:41.000Z</updated>
        <content type="html"><![CDATA[<p>个人认为，快速排序的基本思想就是：
将需要排序的数切分成两段，一段小，一段大，通过递归不断切分，从而完成了排序。</p>
<p>具体步骤如下：</p>
<blockquote>
<ol>
<li>从需要排序的数组中随便选择一个数，作为中间数 ，保存到一个变量 centerValue 中。不过一般为了方便，可直接选择第一个数或者中间那个数为中间数。</li>
<li>从需要排序的数组里删除这个中间数。</li>
<li>定义两个新数组，左数组 leftArray 和右数组 rightArray 。</li>
<li>遍历数组，依次与中间数 centerValue 比较大小。
• 比 centerValue 小的放左数组 leftArray ；
• 比 centerValue 大的放右数组 rightArray ；</li>
<li>使用递归的方式，按以上规则继续对左数组leftArray 和右数组 rightArray 进行划分。</li>
<li>返回排序结果。</li>
<li>为防止递归陷入死循环，需要在函数开头添加一个判断条件，即：如果数组的长度小于等于1，则将这个数组return回去。</li>
</ol>
</blockquote>
<pre><code>function quickSort(array) {
    // 为防止递归陷入死循环
    if(array.length &lt;= 1) {
        return array
    }
    let leftArr = [],
        rightArr = [],
        centerVal = array.shift()
    
    array.forEach(item =&gt; {
        if(item &lt; centerVal) {
            // 如果小于中间值 则push到leftArr里面
            leftArr.push(item)
        }else {
            // 否则push到rightArr里面
            rightArr.push(item)
        }
    });
    
    let leftArrSortRes = quickSort(leftArr)
    let rightArrSortRes = quickSort(rightArr)
    return leftArrSortRes.concat(centerVal, rightArrSortRes)
    // 等同于上面的代码
    // return quickSort(leftArr).concat(centerVal, quickSort(rightArr))
}

let sortArr = quickSort([52,45,4,2,64,21,652,15,1,0,-54,-1,58,7,9])
console.log(sortArr); // [-54, -1, 0, 1, 2, 4, 7, 9, 15, 21, 45, 52, 58, 64, 652]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[手写一个call、apply、bind]]></title>
        <id>https://zouxiulong.github.io/post/shouxie-call-apply-bind</id>
        <link href="https://zouxiulong.github.io/post/shouxie-call-apply-bind">
        </link>
        <updated>2021-03-12T06:04:11.000Z</updated>
        <summary type="html"><![CDATA[<p>为什么要手写代码，不仅仅是为了面试，也是为了帮助我们更好的理解代码逻辑，深入其中原理最好的方法。</p>
]]></summary>
        <content type="html"><![CDATA[<p>为什么要手写代码，不仅仅是为了面试，也是为了帮助我们更好的理解代码逻辑，深入其中原理最好的方法。</p>
<!-- more -->
<p>call 和 apply 都是为了解决改变 this 的指向。作⽤都是相同的，只是传参的⽅式 不同。 除了第⼀个参数外， call 可以接收⼀个参数列表， apply 只接受⼀个参数数组。bind 和其他两个⽅法作⽤也是⼀致的，只是该⽅法会返回⼀个函数。</p>
<h2 id="它们有什么不同">它们有什么不同？</h2>
<blockquote>
<p>它们接收的第一个参数都是函数上下文也就是要绑定的this指向。
apply的第二个参数是一个参数数组，call和bind的第二个及之后的参数作为函数实参按顺序传入。
bind绑定完this的指向后会返回一个新的函数体，不会被立即调用，其他两个会立即调用。</p>
</blockquote>
<p>话不多说，直接上代码</p>
<pre><code>// 公共部分代码
function getValue(from, to){
    console.log( this.name + '年龄' + this.age, '来自' + from + '去往' + to );
}
var data = {
    name: '小邹',
    age: '18'
}
</code></pre>
<h2 id="实现call方法">实现call方法</h2>
<pre><code>Function.prototype.myCall = function(context) {
    //this必须是一个函数
    if(typeof this !== 'function') {
       throw new Error('type Error')
    }
    context = context || window  //context赋值为传入来的对象
    context.fn = this // 一开始的this是指向要调用的那个函数，这里先将这个函数归纳到context对象里面
    let args = Array.from(arguments).slice(1) // 把传入的参数截取出来
    context.fn(...args) //调用这个函数
    delete context.fn
}
</code></pre>
<h2 id="实现apply方法">实现apply方法</h2>
<pre><code>Function.prototype.myApply = function(context) {
    if(typeof this !== 'function') throw new Error('type Error')
    context = context || window
    let args = [...arguments].slice(1)
    context.fn = this
    context.fn(...args[0]) //这里传入的是一个数组，需要将数组每一项传进去
    delete context.fn
}
</code></pre>
<h2 id="实现bind方法">实现bind方法</h2>
<pre><code>Function.prototype.myBind = function(context) {
    if(typeof this !== 'function') throw new Error('type Error')
    context = context || window
    let fn = this // 保存this，这里的this指的是getValue方法
    let args = [...arguments].slice(1)
    return function() {
        let rets = args.concat(Array.from(arguments))
        fn.myApply(context, rets)
    }
}
</code></pre>
<h2 id="测试代码">测试代码</h2>
<pre><code>getValue.myCall(data, '九江', '上海') // 小邹年龄18 来自九江去往上海
getValue.myApply(data, ['九江', '上海']) // 小邹年龄18 来自九江去往上海
getValue.myBind(data, '九江', '上海')(&quot;浦东&quot;) // 小邹年龄18 来自九江去往上海浦东
</code></pre>
<p>收工，你学废了吗？</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[js算法：广度优先遍历（BFS）和深度优先遍历（DFS）]]></title>
        <id>https://zouxiulong.github.io/post/js-BFS-DFS</id>
        <link href="https://zouxiulong.github.io/post/js-BFS-DFS">
        </link>
        <updated>2021-02-26T04:33:15.000Z</updated>
        <content type="html"><![CDATA[<p>先用一个DOM树来进行说明</p>
<pre><code>&lt;div class=&quot;parent&quot;&gt;
        &lt;div class=&quot;child-1&quot;&gt;
            &lt;div class=&quot;child-1-1&quot;&gt;
                &lt;div class=&quot;child-1-1-1&quot;&gt;
                    &lt;div class=&quot;child-1-1-1-1&quot;&gt;a&lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;
            &lt;div class=&quot;child-1-2&quot;&gt;
                &lt;div class=&quot;child-1-2-1&quot;&gt;b&lt;/div&gt;
            &lt;/div&gt;
            &lt;div class=&quot;child-1-3&quot;&gt;c&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;child-2&quot;&gt;
            &lt;div class=&quot;child-2-1&quot;&gt;d&lt;/div&gt;
            &lt;div class=&quot;child-2-2&quot;&gt;e&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;child-3&quot;&gt;
            &lt;div class=&quot;child-3-1&quot;&gt;f&lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
</code></pre>
<h2 id="深度优先遍历">深度优先遍历</h2>
<blockquote>
<p>该方法是以纵向的维度dom进行遍历，从dom根节点开始，一直沿着最近的子节点遍历下去，直到它的所有子节点遍历完毕后再沿着它的兄弟节点进行下去，知道所有节点遍历完成。</p>
</blockquote>
<p>如图所示：
<img src="https://zouxiulong.github.io/post-images/1614661433049.jpg" alt=""></p>
<p>js实现该算法（递归）：</p>
<pre><code>function deepNode(node, nodeList = []) {
    // 判断node是否存在
    if(node) {
        nodeList.push(node)
        for (let i = 0; i &lt; node.children.length; i++) {
            // 每次遍历需要将 子节点 和 已经存储 的子节点 传进去
            deepNode(node.children[i], nodeList)
        }
    }
    return nodeList
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM, []));
</code></pre>
<p>打印结果：
<img src="https://zouxiulong.github.io/post-images/1614662208602.jpg" alt=""></p>
<p>js实现该算法（非递归）：</p>
<pre><code>function deepNode(node) {
    var nodeList = [];  
    if (node) {  
        var queue = [];  
        queue.push(node); 
        while (queue.length != 0) {  
            var childrenItem = queue.pop();  // 取数据最后一个节点
            nodeList.push(childrenItem);  
            var childrenList = childrenItem.children;  
            for (var i = childrenList.length-1; i &gt;= 0; i--)  {
                queue.push(childrenList[i]);  // 有children，向后push数据
            }
        }  
    }    
    return nodeList;  
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM));
</code></pre>
<p>打印结果：
<img src="https://zouxiulong.github.io/post-images/1614668416846.jpg" alt=""></p>
<h2 id="广度优先遍历">广度优先遍历</h2>
<blockquote>
<p>该方法是以横向的维度进行遍历，从一个节点开始，尝试访问尽可能靠近它的目标节点。本质上这种遍历在图上是逐层移动的，首先检查最靠近第一个节点的层，再逐渐向下移动到离起始节点最远的层（犹如爬楼，走完一层的楼梯，继续下一层的楼梯）</p>
</blockquote>
<p>如图所示：
<img src="https://zouxiulong.github.io/post-images/1614663746263.jpg" alt=""></p>
<p>js实现该算法：</p>
<pre><code>function deepNode(node) {
    let nodeList = []
    if(node != null) {
        let queue = [] // 遍历的集合
        queue.push(node)
        // 有值进行遍历，直到条件不满足
        while(queue.length != 0) {
            let item = queue.shift()
            nodeList.push(item)
            let children = item.children
            for (let i = 0; i &lt; children.length; i++) {
                queue.push(children[i])
            }
        }
    }
    return nodeList
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM));
</code></pre>
<p>打印结果：
<img src="https://zouxiulong.github.io/post-images/1614663133808.jpg" alt=""></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[js算法：冒泡排序]]></title>
        <id>https://zouxiulong.github.io/post/js-maopao</id>
        <link href="https://zouxiulong.github.io/post/js-maopao">
        </link>
        <updated>2021-02-25T07:26:05.000Z</updated>
        <summary type="html"><![CDATA[<p>对数组进行 冒泡排序 算是比较简单的，冒泡排序也是容易理解的一种排序算法了，在面试的时候，很可能就会问到。</p>
]]></summary>
        <content type="html"><![CDATA[<p>对数组进行 冒泡排序 算是比较简单的，冒泡排序也是容易理解的一种排序算法了，在面试的时候，很可能就会问到。</p>
<!-- more -->
<h2 id="实现原理">实现原理</h2>
<blockquote>
<p>数组有 n 个数，比较每相邻的两个数，如果前者比后者大，则把两个数位置进行位置交换。一轮下来就可以选出最大的一个数放在最后面；经过 n 轮比较就完成了所有数的排序。
如图所示：
<img src="https://zouxiulong.github.io/post-images/1614670549266.webp" alt=""></p>
</blockquote>
<p>话不多说，直接上代码</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
for(let i = 0; i &lt; arr.length - 1; i++ ) {
    // 如果前一个数 大于 后一个数则交换位置
    if(arr[i] &gt; arr[i+1]) {
        let max = arr[i] // 声明一个变量暂存数据
        arr[i] = arr[i+1]
        arr[i+1] = max
    }
}
console.log(arr)  // [0, 5, 6, 4, 9, 21, 10, 9, -6, 5, 15, -10, 27]  // 第一轮得出最大值
</code></pre>
<p>这里有一个难点，就是 <strong>i &lt; arr.length - 1</strong> ，是因为倒数第二轮进行比较的时候已经得出了最大值，所以 arr.length 可以进减1。</p>
<p>我们继续完善代码</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
// 遍历数组，次数就是arr.length - 1
for (let j = 0; j &lt; arr.length - 1; j++) {
    
    for(let i = 0; i &lt; arr.length - 1; i++ ) {
        console.log(arr[i] , arr[i+1]);
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i]
            arr[i] = arr[i+1]
            arr[i+1] = max
        }
    }
    
}
console.log(arr)  // [-10, -6, 0, 4, 5, 5, 6, 9, 9, 10, 15, 21, 27] 
</code></pre>
<p>我们能找到数组中最大的数，放到最后，这样重复 arr.length - 1 次，便可以实现数组按从小到大的顺序排好了。为什么是 arr.length - 1 次 ，因为倒数第二轮已经拍好了顺序，这里我们可以进行减1，减少不必要的循环。</p>
<p>虽然上面的代码已经实现冒泡排序了，但就像注释中提到的，内层 for 循环的次数写成，i &lt; arr.length - 1 ，是不是合适呢？
我们想一下，当第一次，找到最大数，放到最后，那么下一次，遍历的时候，是不是就不能把最后一个数算上了呢？因为他就是最大的了，不会出现，前一个数比后一个数大，要交换位置的情况，所以内层 for 循环的次数，改成 i &lt; arr.length - 1 - j ，才合适，看下面的代码。</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
for (let j = 0; j &lt; arr.length - 1; j++) {
    // 这里要根据外层for循环的 j，逐渐减少内层 for循环的次数
    for(let i = 0; i &lt; arr.length - 1 - j; i++ ) {
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i]
            arr[i] = arr[i+1]
            arr[i+1] = max
        }
    }
}
</code></pre>
<p>我们想下有种情况，当原数组是：arr = [1,2,4,3]
在经过第一轮冒泡排序之后，数组就变成了：arr = [1,2,3,4]
此时，数组已经排序完成了，但是按上面的代码来看，数组还会继续排序，所以我们加一个标志位，如果某次循环完后，没有任何两数进行交换，就将标志位 设置为 true，表示排序完成，这样我们就可以减少不必要的排序，提高性能。</p>
<p>终极代码：</p>
<pre><code>let arr = [1,2,4,3]
for (let j = 0; j &lt; arr.length - 1; j++) {
    // 声明一个变量，作为标志位
    let flag = true
    for(let i = 0; i &lt; arr.length - 1 - j; i++ ) {
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i] 
            arr[i] = arr[i+1]
            arr[i+1] = max
						flag = false
        }
    }
    if(flag) break
}
</code></pre>
<h2 id="总结">总结</h2>
<p>1、外层 for 循环控制循环次数
2、内层 for 循环进行两数交换，找每次的最大数，排到最后
3、设置一个标志位，减少不必要的循环</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[理解 v-model、v-bind、$attrs、 $listener 和 .sync 及其在跨组件数据双向绑定上的应用]]></title>
        <id>https://zouxiulong.github.io/post/li-jie-v-modelv-binddollarattrs-dollarlistener-he-sync-ji-qi-zai-kua-zu-jian-shu-ju-shuang-xiang-bang-ding-shang-de-ying-yong</id>
        <link href="https://zouxiulong.github.io/post/li-jie-v-modelv-binddollarattrs-dollarlistener-he-sync-ji-qi-zai-kua-zu-jian-shu-ju-shuang-xiang-bang-ding-shang-de-ying-yong">
        </link>
        <updated>2020-12-21T02:14:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-v-model-的本质">1. v-model 的本质</h2>
<pre><code class="language-text">&lt;MyList v-model=&quot;lovingVue&quot;&gt;&lt;/MyList&gt;
 
&lt;!-- 其实是以下的语法糖 --&gt;
&lt;MyList :value=&quot;lovingVue&quot; @input=&quot;(data) =&gt; lovingVue = data&quot;&gt;&lt;/MyList&gt;
</code></pre>
<p>没耐心理解代码的话，直接看下面的文字：
v-model 的本质是：父组件给子组件传一个名为 value 的prop，然后对子组件挂载一个名为 input 的事件监听。
当子组件手动 emit 这个input 事件时，携带的载荷自动赋值到v-model后绑的父组件变量上。
（所以其实不是自动双向绑定，还是需要手动emit input事件的。）</p>
<h2 id="2-v-bind的本质">2. v-bind的本质</h2>
<p>本质是 批量传入props</p>
<p>例如：</p>
<pre><code class="language-text">&lt;Component v-bind=&quot;{a: foo, b: bar, c: baz}&quot;&gt;

&lt;!-- 相当于 --&gt;
&lt;Component  :a=foo :b=bar :c=baz&gt;
</code></pre>
<h2 id="3-attrs的本质">3. $attrs的本质</h2>
<p>官方解释：
包含了父作用域中不作为 prop 被识别 (且获取) 的特性绑定 (class 和 style 除外)。当一个组件没有声明任何 prop 时，这里会包含所有父作用域的绑定 (class 和 style 除外)，并且可以通过 v-bind=&quot;$attrs&quot; 传入内部组件——在创建高级别的组件时非常有用。</p>
<p>我的理解：
接收除了props声明外的所有绑定属性（class、style除外）</p>
<p>父组件以形如:foo=&quot;xxx&quot;或者v-bind=&quot;{age:12}&quot;传给子组件的属性，但凡没有被子组件的props接收的，都会被扔到子组件的$attrs里去。</p>
<h2 id="4-listeners的本质">4. $listeners的本质</h2>
<p>官方解释：
包含了父作用域中的 (不含 .native 修饰器的) v-on 事件监听器。它可以通过 v-on=&quot;$listeners&quot; 传入内部组件——在创建更高层次的组件时非常有用。</p>
<p>我的理解：
接收除了带有.native事件修饰符的所有事件监听器</p>
<p>父组件以 @eventName=&quot;fn&quot; 或者 v-on:eventName=&quot;fn&quot; 对子组件挂载事件监听。对子组件而言，父组件监听的事件都放在$listeners里。</p>
<p>如果子组件对后代组件使用v-on=&quot;$listeners&quot;，相当于对后代组件批量挂载了父组件对自己的事件监听。因此后代组件的emit会触发父组件的事件方法。</p>
<p>-----------------------------------------------------------------------------------------------------------------</br>
（以下关于 vue 事件内部实现，前后文无关，可跳过。）</p>
<p>打印 $listeners，可以看到如下结构。是一个 事件名+回调函数（数组） 构成的对象。</p>
<p><code>图中是后代组件的 $listeners，发现当在子组件中，如果对后代组件 v-on=&quot;$listeners&quot; 的同时，新增同名监听如@on-page-change='xxx'，那么父组件的回调、子组件的回调，都会被加进后代组件的回调函数栈中。</code></p>
<p><img src="https://zouxiulong.github.io/post-images/1610504445667.png" alt=""></p>
<p>这说明，vue on-emit 事件的本质是 父组件 向子组件传递事件对象，对象由事件名和回调函数构成。子组件会根据传入，维护自己的 $listener 对象，对各事件创建或 push 回调函数栈。</p>
<p>当子组件执行$emit，是在依次弹出栈内回调函数，并执行。是交由子组件管理的范畴，父组件无感知。是典型的观察者模式。</p>
<h2 id="5-sync的本质">5. .sync的本质</h2>
<pre><code>&lt;Component  :foo=&quot;val&quot; @update:foo=&quot;(payload)=&gt;{ val = payload }&quot;&gt;

&lt;!-- 实质是 语法糖 --&gt;
&lt;Component :foo.sync=&quot;val&quot;&gt;
</code></pre>
<pre><code>// 子组件使用
this.$emit('update:foo', payload)
</code></pre>
<p>实质是更方便地实现数据双向绑定。即数据流向父-&gt;子天然实现，子-&gt;父只需子组件emit相关事件即可，从而实现双向绑定。</p>
<p>那么掌握了以上知识了之后，我们如何实现跨越三层的一个双向绑定呢？</p>
<pre><code>&lt;!-- A Component --&gt;
&lt;template&gt;
    &lt;BComponent v-model=&quot;modalShow&quot;&gt;&lt;/BComponent&gt;
&lt;/template&gt;
 
&lt;script&gt;
    this.modalShow = true // 往下传递状态，直接到C
&lt;/script&gt;
 
 
&lt;!-- B Component --&gt;
&lt;template&gt;
    &lt;CComponent v-bind=&quot;$attrs&quot; v-on=&quot;$listeners&quot;&gt;&lt;/CComponent&gt;
&lt;/template&gt;
 
&lt;script&gt;
    // 中间组件也可以中途向两边更改状态,一致性需要手动保持
    // this.$attrs.value = true
    // this.$emit('input', true)
&lt;/script&gt;
 
 
&lt;!-- C Component --&gt;
&lt;script&gt;
    @Props()
    value:boolean
    
    handleClose(){
        this.$emit('input', false) // 往上emit状态，直接到A
    }
&lt;/script&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JS 闭包]]></title>
        <id>https://zouxiulong.github.io/post/bibao</id>
        <link href="https://zouxiulong.github.io/post/bibao">
        </link>
        <updated>2020-09-29T02:12:36.000Z</updated>
        <summary type="html"><![CDATA[<p>闭包（closure）是Javascript语言的一个难点，也是它的特色，很多高级应用都要依靠闭包实现。</p>
]]></summary>
        <content type="html"><![CDATA[<p>闭包（closure）是Javascript语言的一个难点，也是它的特色，很多高级应用都要依靠闭包实现。</p>
<!-- more --> 
<p>简单来说，闭包就是函数中的函数，就是说一个函数要访问另外一个目标函数内部的变量，就要在目标函数中再定义一个函数（以此来把作用域链往下延长一段，目的就是为了利用js在找自由变量时,会沿着作用域链一级一级往上找的特点），并将这个定义的函数return出来，供外部使用。</p>
<h2 id="javascript变量的作用域">Javascript变量的作用域</h2>
<p>有点类似于原型链（proto chain），Javascript中变量遵从作用域链（scope chain）规则。</p>
<p><img src="https://zouxiulong.github.io/post-images/1612752659326.webp" alt=""></p>
<p>如上图所示，在Javascript中，每一个函数体对应于一个作用域。当访问一个变量时，我们会先访问当前作用域内是否有定义该变量，如果没有就会在该作用域外的作用域内寻找是否有改变量，依此类推，一直寻找到全局变量。如果全局变量中依旧没有定义该变量，就会返回undefined。</p>
<p>我们来看下下面这个栗子：</p>
<pre><code>var a = '外面的特仑苏'
function foo1() {
    var a = '里面的特仑苏'
    console.log('我要喝' + a)   //我要喝里面的特仑苏
}
function foo2() {
    console.log('我要喝' + a)   //我要喝外面的特仑苏
}
foo1()
foo2()
</code></pre>
<p>在上述例子中，我们在 foo1 函数体内定义了变量a，因此 foo1 在寻找完当前作用域即可以得到里面的特仑苏，而在 foo2 函数体内没有定义变量a，它会沿着作用域链去寻找全局变量，然后得到了外面的特仑苏。</p>
</br>
<p>在函数外部无法读取函数内的局部变量。函数内部声明变量的时候，一定要使用var命令。如果不用的话，你实际上声明了一个全局变量！</p>
<pre><code>function foo3(){　　　　
  var a='里面的特仑苏';　　
}
alert(a); // error
</code></pre>
<pre><code>function foo4(){　　　　
  a='里面的特仑苏';　　
}
foo4();
alert(a); // 里面的特仑苏
</code></pre>
<h2 id="如何从外部读取局部变量">如何从外部读取局部变量？</h2>
<p>出于种种原因，我们有时候需要得到函数内的局部变量。但是，前面已经说过了，正常情况下，这是办不到的，只有通过变通方法才能实现。</p>
<p>那就是在函数的内部，再定义一个函数。</p>
<pre><code>function f1(){
　　a=999;
　　function f2(){
　　　　alert(a); // 999
　　}
}
</code></pre>
<p>在上面的代码中，函数 f2 就被包括在函数 f1 内部，这时 f1 内部的所有局部变量，对 f2 都是可见的。但是反过来就不行，f2 内部的局部变量，对 f1 就是不可见的。这就是Javascript语言特有的“链式作用域”结构（chain scope），</p>
<p>子对象会一级一级地向上寻找所有父对象的变量。所以，父对象的所有变量，对子对象都是可见的，反之则不成立。</p>
<p>既然 f2 可以读取 f1 中的局部变量，那么只要把 f2 作为返回值，我们不就可以在 f1 外部读取它的内部变量了吗！</p>
<pre><code>function f1(){
  a=999;
  function f2(){
　  alert(a);
  }
  return f2;
}
var foo=f1();
foo(); // 999
</code></pre>
<h2 id="闭包的概念">闭包的概念</h2>
<p>上一节代码中的f2函数，就是闭包。</p>
<p>各种专业文献上的“闭包”（closure）定义非常抽象，很难看懂。我的理解是，闭包就是能够读取其他函数内部变量的函数。</p>
<p>由于在Javascript语言中，只有函数内部的子函数才能读取局部变量，因此可以把闭包简单理解成“定义在一个函数内部的函数”。</p>
<p>所以，在本质上，闭包就是将函数内部和函数外部连接起来的一座桥梁。</p>
<h2 id="说到闭包就得提什么是gc垃圾回收机制">说到闭包就得提什么是GC（垃圾回收机制）</h2>
<p>简单来说：</p>
<ol>
<li>如果变量没有被引用JS会隔一段时间周期性的释放变量占用的内存空间。</li>
<li>全局变量不会被回收，只有浏览器卸载页面时才会结束。</li>
</ol>
<pre><code>function f1() {
    var x = 1
    var y = 100
    console.log(y)
    y++
    return function f2() {
        console.log(x++)
    }
}
var foo= f1()
// 100
foo() // 1
foo() // 2
</code></pre>
<p>当执行 foo() 时为什么输出x累加呢？y为什么不输出，这里的y没有被全局变量引用所以被JS垃圾回收销毁所以不会输出。
多次执行 foo() 输出累加的x说明x并没有被销毁，因为 f2 函数被映射到了全局变量 foo 上，而 f2 函数又依赖x变量，所以 f1 函数中的x并不会被销毁。</p>
<h2 id="闭包应用">闭包应用：</h2>
<p>在实际开发中，闭包主要是用来封装变量实现公有私有变量，收敛权限。</p>
<p>优点：</p>
<ol>
<li>使变量的值始终保持在内存中。</li>
<li>避免全局变量的污染。</li>
<li>私有成员的存在。
缺点：</li>
<li>因为不会被GC回收，所以常驻内存，会增大内存的使用量，使用不当会造成内存泄露。</li>
</ol>
</br>
<h2 id="使用闭包的注意点">使用闭包的注意点</h2>
<p>因为闭包会使得函数中的变量都被保存在内存中，内存消耗很大，所以不能滥用闭包，否则可能会造成内存泄露。解决方法是，在使用完闭包函数之后，将变量设置为null或者undefined。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaScript 中 call()、apply()、bind() 的用法]]></title>
        <id>https://zouxiulong.github.io/post/callapplybind</id>
        <link href="https://zouxiulong.github.io/post/callapplybind">
        </link>
        <updated>2020-09-18T09:07:44.000Z</updated>
        <summary type="html"><![CDATA[<p>在JavaScript中，bind、call和apply作用是一样的，都是为了改变某个函数运行时的上下文（context）而存在的，换句话说，就是为了改变函数体内部this的指向。</p>
]]></summary>
        <content type="html"><![CDATA[<p>在JavaScript中，bind、call和apply作用是一样的，都是为了改变某个函数运行时的上下文（context）而存在的，换句话说，就是为了改变函数体内部this的指向。</p>
<!-- more -->
<p>其实是一个很简单的东西，认真看十分钟就从一脸懵B 到完全 理解！
先看明白下面例子：
例1</p>
<pre><code>var name = '小邹',age = '18';
let obj={
		name: '小李',
		objAge: this.age,
		_fun: function(){
			console.log( this.name + '年龄' + this.age );
		}
}

obj.age;  // 25
obj._fun()  // 小李年龄 undefined
</code></pre>
<p>例2</p>
<pre><code>var name = '小李'
function _fun(){
		console.log( this.name );
}
_fun() // 小李
</code></pre>
<p>比较一下这两者 this 的差别，第一个打印里面的 this 指向 obj，第二个全局声明的 fun() 函数 this 是 window ；</p>
<h2 id="call-和-apply-的共同点">call 和 apply 的共同点</h2>
<p>它们的共同点是，都能够改变函数执行时的上下文，将一个对象的方法交给另一个对象来执行，并且是立即执行的。</p>
<p>为何要改变执行上下文？举一个生活中的小例子：平时没时间做饭的我，周末想给孩子炖个腌笃鲜尝尝。但是没有适合的锅，而我又不想出去买。所以就问邻居借了一个锅来用，这样既达到了目的，又节省了开支，一举两得。</p>
<p>改变执行上下文也是一样的，A 对象有一个方法，而 B 对象因为某种原因，也需要用到同样的方法，那么这时候我们是单独为 B 对象扩展一个方法呢，还是借用一下 A 对象的方法呢？当然是借用 A 对象的啦，既完成了需求，又减少了内存的占用。</p>
<p>另外，它们的写法也很类似，调用 call 和 apply 的对象，必须是一个函数 Function。</p>
<h2 id="call-和-apply-的区别">call 和 apply 的区别</h2>
<h3 id="call-的写法">call 的写法</h3>
<pre><code>Function.call(obj, param1, param2, ...paramN )
</code></pre>
<p>需要注意以下几点：</p>
<ul>
<li>调用 call 的对象，必须是个函数 Function。</li>
<li>call 的第一个参数，是一个对象。 Function 的调用者，将会指向这个对象。如果不传，则默认为全局对象 window。</li>
<li>第二个参数开始，可以接收任意个参数。每个参数会映射到相应位置的 Function 的参数上。但是如果将所有的参数作为数组传入，它们会作为一个整体映射到 Function 对应的第一个参数上，之后参数都为空。</li>
</ul>
<pre><code>function fun (a,b,c) {}

fun.call(obj, 1,2,3)
// fun 接收到的参数实际上是 1,2,3

fun.call(obj, [1,2,3])
// fun 接收到的参数实际上是 [1,2,3],undefined,undefined
</code></pre>
<h3 id="apply-的写法">apply 的写法</h3>
<pre><code>Function.apply(obj, Array)
</code></pre>
<p>需要注意的是：</p>
<ul>
<li>它的调用者必须是函数 Function，并且只接收<strong>两个参数</strong>，第一个参数的规则与 call 一致。</li>
<li>第二个参数，必须是<strong>数组或者类数组</strong>，它们会被转换成类数组，传入 Function 中，并且会被映射到 Function 对应的参数上。这也是 call 和 apply 之间，很重要的一个区别。</li>
</ul>
<pre><code>function fun (a,b,c) {}

fun.apply(obj, [1,2,3])
// func 接收到的参数实际上是 1,2,3

fun.apply(obj, {
    0: 1,
    1: 2,
    2: 3,
    length: 3
})
// fun 接收到的参数实际上是 1,2,3
</code></pre>
<h5 id="什么是类数组">什么是类数组？</h5>
<p>先说数组，这我们都熟悉。它的特征有：可以通过角标调用，如 array[0]；具有长度属性length；可以通过 for 循环或forEach方法，进行遍历。</p>
<p>那么，类数组是什么呢？顾名思义，就是具备与数组特征类似的对象。比如，下面的这个对象，就是一个类数组。</p>
<pre><code>var arrayLike = {
    0: 1,
    1: 2,
    2: 3,
    length: 3
};
</code></pre>
<p>类数组 arrayLike 可以通过角标进行调用，具有length属性，同时也可以通过 for 循环进行遍历。</p>
<p>类数组，还是比较常用的，只是我们平时可能没注意到。比如，我们获取 DOM 节点的方法，返回的就是一个类数组。再比如，在一个方法中使用 arguments 获取到的所有参数，也是一个类数组。</p>
<p>但是需要注意的是：<strong>类数组无法使用 forEach、splice、push 等数组原型链上的方法</strong>，毕竟它不是真正的数组。</p>
<h2 id="bind-的使用">bind 的使用</h2>
<p>最后来说说 bind。在 MDN 上的解释是：bind() 方法创建一个新的函数，在调用时设置 this 关键字为提供的值。并在调用新函数时，将给定参数列表作为原函数的参数序列的前若干项。</p>
<p>它的语法如下：</p>
<pre><code>Function.bind(thisArg, arg1, arg2, ...)
</code></pre>
<p>bind 方法 与 apply 和 call 比较类似，也能改变函数体内的 this 指向。不同的是，bind 方法的返回值是函数，并且需要稍后调用，才会执行。而 apply 和 call 则是立即调用。</p>
<p>来看下面这个例子：</p>
<pre><code>function add (a, b) {
    return a + b;
}

function sub (a, b) {
    return a - b;
}

add.bind(sub, 5, 3); // 这时，并不会返回 8
add.bind(sub, 5, 3)(); // 调用后，返回 8
</code></pre>
<p>如果 bind 的第一个参数是 null 或者 undefined，this 就指向全局对象 window。</p>
<h2 id="call-apply-bind-都是用来重定义-this-这个对象的">call()、apply()、bind() 都是用来重定义 this 这个对象的</h2>
<p>比如：</p>
<pre><code>let obj={
		name: '小李',
		objAge: this.age,
		_fun: function(){
				console.log( this.name + '年龄' + this.age );
		}
}
var db = {
		name: '小邹',
		age: '18'
}

obj._fun.call(db)；　　 // 小邹年龄 18
obj._fun.apply(db);　　// 小邹年龄 18
obj._fun.bind(db)();　   // 小邹年龄 18
</code></pre>
<p>call 和 apply 的主要作用，是改变对象的执行上下文，并且是立即执行的。它们在参数上的写法略有区别。</p>
<p>bind 也能改变对象的执行上下文，它与 call 和 apply 不同的是，返回值是一个函数，并且需要稍后再调用一下，才会执行。</p>
<h2 id="对比call-bind-apply-传参情况下">对比call 、bind 、 apply 传参情况下</h2>
<pre><code>let obj={
		name: '小李',
		objAge: this.age,
		_fun: function( f, t ){
				console.log( this.name + '年龄' + this.age, '来自' + f + '去往' + t );
		}
}
var db = {
		name: '小邹',
		age: '18'
}

obj._fun.call(db, '九江', '上海')；　　 // 小邹年龄 18  来自 九江去往上海
obj._fun.apply(db, ['九江', '上海']);　　// 小邹年龄 18  来自 九江去往上海
obj._fun.bind(db, '九江', '上海')();　   // 小邹年龄 18  来自 九江去往上海
obj._fun.bind(db, ['九江', '上海'])();　   // 小邹年龄 18  来自 九江, 上海去往 undefined
</code></pre>
<p>微妙的差距！</p>
<p>从上面四个结果不难看出:</p>
<p>call 、bind 、 apply 这三个函数的第一个参数都是 this 的指向对象，第二个参数差别就来了：</p>
<p>call 的参数是直接放进去的，第二第三第 n 个参数全都用逗号分隔，直接放到后面 <strong>obj._fun.call(db, '九江', ... ,'string' )</strong>。</p>
<p>apply 的所有参数都必须放在一个数组里面传进去 <strong>obj._fun.apply(db, ['九江', ..., 'string' ])</strong>。</p>
<p>bind 除了返回是函数以外，它 的参数和 call 一样。</p>
<p>当然，三者的参数不限定是 string 类型，允许是各种类型，包括函数 、 object 等等！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[forEach、map、every、some和filter的用法]]></title>
        <id>https://zouxiulong.github.io/post/foreachmapeverysome-he-filter-de-yong-fa</id>
        <link href="https://zouxiulong.github.io/post/foreachmapeverysome-he-filter-de-yong-fa">
        </link>
        <updated>2020-08-14T06:26:51.000Z</updated>
        <summary type="html"><![CDATA[<p>在Javascript中，如何处理数组中的每一项数据？</p>
<p>有人可能会说，这还不简单，直接一个for循环遍历一下就好了。
是的，确实，这是最常见的做法。
但是，除此之外，ES5还提供了处理数组更加方便的方法，如题。
接下来，我将通过几个简单的实例来具体讲解这几个方法。</p>
]]></summary>
        <content type="html"><![CDATA[<p>在Javascript中，如何处理数组中的每一项数据？</p>
<p>有人可能会说，这还不简单，直接一个for循环遍历一下就好了。
是的，确实，这是最常见的做法。
但是，除此之外，ES5还提供了处理数组更加方便的方法，如题。
接下来，我将通过几个简单的实例来具体讲解这几个方法。</p>
<!-- more -->
<h3 id="1-foreach用于遍历数组无返回值">1. forEach，用于遍历数组，无返回值</h3>
<p>这里先给出一个数组（以下例子通用）：</p>
<pre><code>var arr = [1,-2,3,4,-5];
</code></pre>
<p>然后我要做事情的就是，将数组中的每一项翻倍。</p>
<pre><code>arr.forEach(function(item, index, array){
    array[index] = item * 2;
});
console.log(arr);   // [2,-4,6,8,-10]
</code></pre>
<p>可以看到，forEach 可以传入一个匿名函数作为参数，而该匿名函数有含有三个参数，其依次代表数组遍历时的当前元素item，数组遍历时的当前元素的索引index，以及正在遍历的数组array。有了这三个参数，可以方便我们做很多事情，比如说示例当中将每一项数组元素翻倍，这时需要用到第一个参数item。但是，仅仅只是将item乘以2可不行，我们还得将其赋值给原来的数组，这时我们就得用到后面两个参数index和array。
根据上述可知，array[index]是全等于item的。</p>
<pre><code>arr.forEach(function(item, index, array){
    console.log(array[index] === item);   // true
});
</code></pre>
<h3 id="2-map用于遍历数组返回处理之后的新数组">2. map，用于遍历数组，返回处理之后的新数组</h3>
<pre><code>var newArr = arr.map(function(item, index, array){
    return item * 2;
});
console.log(newArr);   // [2,-4,6,8,-10]
</code></pre>
<p>可以看到，该方法与 forEach 的功能类似，只不过 map 具有返回值，会返回一个新的数组，这样处理数组后也不会影响到原有数组。</p>
<h3 id="3-every用于判断数组中的每一项元素是否都满足条件返回一个布尔值">3. every，用于判断数组中的每一项元素是否都满足条件，返回一个布尔值</h3>
<pre><code>var isEvery = arr.every(function(item, index, array){
    return item &gt; 0;
});
console.log(isEvery);   // false
</code></pre>
<p>可以看到，示例中是要判断数组arr中的元素是否都为正数，很显然不是，所以该方法最终返回false。</p>
<h3 id="4-some用于判断数组中的是否存在满足条件的元素返回一个布尔值">4. some，用于判断数组中的是否存在满足条件的元素，返回一个布尔值</h3>
<pre><code>var isSome = arr.some(function(item, index, array){
    return item &lt; 0;
});
console.log(isSome);   // true
</code></pre>
<p>可以看到，该方法与 every 类似，示例中是要判断数组arr中是否存在负数元素，很显然存在，所以该方法最终返回true。</p>
<h3 id="5-filter用于筛选数组中满足条件的元素返回一个筛选后的新数组">5. filter，用于筛选数组中满足条件的元素，返回一个筛选后的新数组</h3>
<pre><code>var minus = arr.filter(function(item, index, array){
    return item &lt; 0;
});
console.log(minus);   // [-2, -5]
</code></pre>
<p>可以看到，示例中是要筛选出数组arr中的所有负数，所以该方法最终返回一个筛选后的新数组[-2, -5]。</p>
<h2 id="补充">补充</h2>
<p>以上五大方法除了传递一个匿名函数作为参数之外，还可以传第二个参数，该参数用于指定匿名函数内的this指向，例如：</p>
<pre><code>// 只传一个匿名函数
arr.forEach(function(item, index, array){
    console.log(this);  // window
});
// 传两个参数
arr.forEach(function(item, index, array){
    console.log(this);  // [1, -2, 3, 4, -5]
},arr);
</code></pre>
<h2 id="重点总结">重点总结</h2>
<ol>
<li>forEach 无返回值，map 和 filter 返回新数组，every 和 some 返回布尔值</li>
<li>匿名函数中this指向默认为window，可通过传第二参数来更改之</li>
<li>五种遍历方法均为ES5方法</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JS中 reduce() 的用法]]></title>
        <id>https://zouxiulong.github.io/post/js-zhong-reduce-de-yong-fa</id>
        <link href="https://zouxiulong.github.io/post/js-zhong-reduce-de-yong-fa">
        </link>
        <updated>2020-08-12T02:38:15.000Z</updated>
        <summary type="html"><![CDATA[<p>过去有很长一段时间，我一直很难理解 reduce() 这个方法的具体用法，平时也很少用到它。事实上，如果你能真正了解它的话，其实在很多地方我们都可以用得上，那么今天我们就来简单聊聊JS中 reduce() 的用法。</p>
]]></summary>
        <content type="html"><![CDATA[<p>过去有很长一段时间，我一直很难理解 reduce() 这个方法的具体用法，平时也很少用到它。事实上，如果你能真正了解它的话，其实在很多地方我们都可以用得上，那么今天我们就来简单聊聊JS中 reduce() 的用法。</p>
<!-- more -->
<h2 id="1-语法">1. 语法</h2>
<pre><code>let arr = [1, 2, 3, 4]
arr.reduce(function(prev,cur,index,arr){
...
}, init)
</code></pre>
<p>arr 表示原数组；
prev 表示上一次调用回调时的返回值，或者初始值 init;
cur 表示当前正在处理的数组元素；
index 表示当前正在处理的数组元素的索引，若提供 init 值，则索引为0，否则索引为1；
init 表示初始值。</p>
<p>其实常用的参数只有两个：prev 和 cur。接下来我们跟着实例来看看具体用法吧~</p>
<h2 id="2-实例">2. 实例</h2>
<p>实现以下需求的方式有很多，其中就包含使用reduce()的求解方式，也算是实现起来比较简洁的一种吧。</p>
<h3 id="1-数组中每个元素出现的次数">1. 数组中每个元素出现的次数</h3>
<pre><code>let arr = ['A', 'B', 'C', 'A', 'D', 'C'];
let arrNum = arr.reduce((prev,cur)=&gt;{
	// in  用法表示 prev 对象是否含有 cur 属性
  if(cur in prev){
    pre[cur]++
  }else{
    pre[cur] = 1 
  }
  return pre
},{})
console.log(arrNum); // {A: 2, B: 1, C: 2, D: 1}
</code></pre>
<h3 id="2数组求和">2.数组求和</h3>
<pre><code>let arr = [2, 1, 3, 4, 2, 5, 6, 5]
let sum = arr.reduce((prev, cur) =&gt; {
		return prev + cur;
})
// 设定初始值求和
let sum = arr.reduce((prev, cur) =&gt; {
		return prev + cur
}, 10);  // 以10为初始值求和
</code></pre>
<p>由于传入了初始值0，所以开始时prev的值为0，cur的值为数组第一项2，相加之后返回值为2作为下一轮回调的prev值，然后再继续与下一个数组项相加，以此类推，直至完成所有数组项的和并返回。</p>
<pre><code>// 对象里的属性求和
var result = [
  { subject: 'math', score: 10 },
  { subject: 'chinese', score: 20 },
  { subject: 'english', score: 30 }
];
var sum = result.reduce((prev, cur) =&gt; {
    return cur.score + prev;
}, 0);
console.log(sum) //60
</code></pre>
<h3 id="2-求数组最大值">2. 求数组最大值</h3>
<pre><code>let arr = [2, 1, 3, 4, 2, 5, 6, 5]
// 方法一
let max = arr.reduce((prev, cur) =&gt; {
		return Math.max(prev,  cur)
})
// 方法二
let max = arr.reduce((pre,cur){
		return pre&gt;cur ? pre : cur;
}); // 342
</code></pre>
<p>由于未传入初始值，所以开始时prev的值为数组第一项2，cur的值为数组第二项1，取两值最大值后继续进入下一轮回调。</p>
<h3 id="3-数组去重">3. 数组去重</h3>
<pre><code>let arr = [2, 1, 3, 4, 2, 5, 6, 5]
let newArr = arr.reduce((prev, cur) =&gt;{
	return prev.indexOf(cur) === -1 &amp;&amp; prev.push(cur)
}, [])
</code></pre>
<p>实现的基本原理如下：</p>
<ol>
<li>初始化一个空数组</li>
<li>将需要去重处理的数组中的第1项在<strong>初始化数组</strong>中查找，如果找不到（空数组中肯定找不到），就将该项添加到<strong>初始化数组</strong>中</li>
<li>将需要去重处理的数组中的第2项在<strong>初始化数组</strong>中查找，如果找不到，就将该项继续添加到<strong>初始化数组</strong>中</li>
<li>……</li>
<li>将需要去重处理的数组中的第n项在<strong>初始化数组</strong>中查找，如果找不到，就将该项继续添加到<strong>初始化数组</strong>中</li>
<li>将这个<strong>初始化数组</strong>返回</li>
</ol>
<h3 id="5-多维数组转化为一维">5. 多维数组转化为一维</h3>
<ol>
<li>将二维数组转化为一维</li>
</ol>
<pre><code>let arr = [[0, 1], [2, 3], [4, 5]]
let newArr = arr.reduce((prev, cur) =&gt; {
    return prev.concat(cur)
}, [])
console.log(newArr); // [0, 1, 2, 3, 4, 5]
</code></pre>
<ol start="2">
<li>将多维数组转化为一维</li>
</ol>
<pre><code>let arr = [[0, 1], [2, 3], [4,[5,6,7]]]
const newArr = function(arr){
   return arr.reduce((prev , cur) =&gt; prev.concat(Array.isArray(cur) ? newArr(cur) : cur), [])
}
console.log(newArr(arr)); //[0, 1, 2, 3, 4, 5, 6, 7]
</code></pre>
<h2 id="3-其他相关方法">3. 其他相关方法</h2>
<h3 id="1-reduceright">1. reduceRight()</h3>
<p>该方法用法与reduce()其实是相同的，只是遍历的顺序相反，它是从数组的最后一项开始，向前遍历到第一项。</p>
<h3 id="2-foreach-map-every-some和filter">2. forEach()、map()、every()、some()和filter()</h3>
<p>详情请戳→ <a href="https://zouxiulong.github.io/post/foreachmapeverysome-he-filter-de-yong-fa">forEach、map、every、some和filter的用法</a>
<br/></p>
<h2 id="重点总结">重点总结：</h2>
<p>reduce() 是数组的归并方法，与forEach()、map()、filter()等迭代方法一样都会对数组每一项进行遍历，但是reduce() 可同时将前面数组项遍历产生的结果与当前遍历项进行运算，这一点是其他迭代方法无法企及的</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[git总结]]></title>
        <id>https://zouxiulong.github.io/post/git</id>
        <link href="https://zouxiulong.github.io/post/git">
        </link>
        <updated>2020-07-01T01:43:47.000Z</updated>
        <summary type="html"><![CDATA[<p>每次git都在百度，终于这次整理了一下自己当前所学的git知识。这篇文章只是我的个人总结方便记忆用的，并不适合初学者来学习git。</p>
]]></summary>
        <content type="html"><![CDATA[<p>每次git都在百度，终于这次整理了一下自己当前所学的git知识。这篇文章只是我的个人总结方便记忆用的，并不适合初学者来学习git。</p>
<!-- more -->
<h3 id="1-创建版本库">1. 创建版本库</h3>
<p>执行 git init ，把当前目录变成Git可以管理的仓库，可以发现当前目录下多了一个.git的目录，个目录是Git来跟踪管理版本库的，没事千万不要手动修改这个目录里面的文件，不然改乱了，就把Git仓库给破坏了。</p>
<p>如果你没有看到.git目录，那是因为这个目录默认是隐藏的，用 ls -ah 或 ls -la 命令就可以看见。</p>
<h3 id="2-添加文件到git仓库">2. 添加文件到Git仓库</h3>
<ul>
<li>第一步，使用命令 git add <file> ，注意，可反复多次使用，添加多个文件；</li>
<li>第二步，使用命令 git commit -m &quot;本次提交说明&quot; ,完成。
例如：</li>
</ul>
<pre><code class="language-text">$ git add file1.txt
$ git add file2.txt file3.txt
$ git commit -m &quot;add 3 files&quot;
</code></pre>
<h3 id="3-添加文件到git仓库">3. 添加文件到Git仓库</h3>
<ul>
<li>要随时掌握工作区的状态，使用git status命令。</li>
<li>如果git status告诉你有文件被修改过，用git diff可以查看修改内容。</li>
</ul>
<h3 id="4-版本回退">4. 版本回退</h3>
<ul>
<li>可以通过 git log 查看提交版本记录。</li>
<li>在Git中，用HEAD表示当前版本，上一个版本就是HEAD^ ，上上一个版本就是HEAD^^</li>
<li>要把当前版本回退到上一版本以使用git reset命令。</li>
</ul>
<pre><code class="language-text">$ git reset --hard HEAD^
</code></pre>
<ul>
<li>回到未来的某个版本</li>
</ul>
<pre><code class="language-text">$ git reset --hard 329782f  //其中329782f是版本的commit id
</code></pre>
<ul>
<li>Git提供了一个命令git reflog用来记录你的每一次命令</li>
</ul>
<h4 id="小结">小结：</h4>
<ul>
<li>HEAD指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令git reset --hard commit_id。</li>
<li>穿梭前，用git log可以查看提交历史，以便确定要回退到哪个版本。</li>
<li>要重返未来，用git reflog查看命令历史，以便确定要回到未来的哪个版本。</li>
</ul>
<h3 id="5-工作区和暂存区">5. 工作区和暂存区</h3>
<ul>
<li>
<h4 id="工作区working-directory">工作区（Working Directory）</h4>
</li>
</ul>
<p>就是你在电脑里能看到的目录，比如我的learngit文件夹就是一个工作区</p>
<ul>
<li>
<h4 id="版本库repository">版本库（Repository）</h4>
</li>
</ul>
<p>工作区有一个隐藏目录.git，这个不算工作区，而是Git的版本库。</p>
<p>Git的版本库里存了很多东西，其中最重要的就是称为stage（或者叫index）的暂存区，还有Git为我们自动创建的第一个分支master，以及指向master的一个指针叫HEAD。</p>
<p><img src="https://zouxiulong.github.io/post-images/1593570454964.webp" alt=""></p>
<p>前面讲了我们把文件往Git版本库里添加的时候，是分两步执行的：</p>
<ul>
<li>第一步是用git add把文件添加进去，实际上就是把文件修改添加到暂存区；</li>
<li>第二步是用git commit提交更改，实际上就是把暂存区的所有内容提交到当前分支。</li>
<li>因为我们创建Git版本库时，Git自动为我们创建了唯一一个master分支，所以，现在git commit就是往master分支上提交更改。</li>
<li>你可以简单理解为，需要提交的文件修改通通放到暂存区，然后一次性提交暂存区的所有修改。</li>
</ul>
<h3 id="6-撤销修改">6. 撤销修改</h3>
<ul>
<li>
<p>场景1：当你改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，用命令git checkout -- file。</p>
</li>
<li>
<p>场景2：当你不但改乱了工作区某个文件的内容，还添加到了暂存区时，想丢弃修改，分两步，第一步用命令git reset HEAD file，就回到了场景1，第二步按场景1操作。</p>
</li>
<li>
<p>场景3：已经提交了不合适的修改到版本库时，想要撤销本次提交，参考版本回退一节，不过前提是没有推送到远程库。</p>
</li>
</ul>
<h3 id="7-删除文件">7. 删除文件</h3>
<p>从版本库中删除该文件，那就用命令git rm删掉，并且git commit
git checkout其实是用版本库里的版本替换工作区的版本，无论工作区是修改还是删除，都可以“一键还原”。</p>
<h3 id="8-分支管理">8. 分支管理</h3>
<ul>
<li>查看分支：git branch</li>
<li>查看远程分支 git branch -r</li>
<li>创建分支：git branch <name></li>
<li>切换分支：git checkout <name></li>
<li>创建+切换分支：git checkout -b <name></li>
<li>合并某分支到当前分支：git merge <name></li>
<li>删除分支：git branch -d <name></li>
<li>查看远程库信息，使用git remote -v；</li>
</ul>
<h3 id="9-多人协作">9. 多人协作</h3>
<p>本地新建的分支如果不推送到远程，对其他人就是不可见的；
从本地推送分支，使用git push origin branch-name，如果推送失败，先用git pull抓取远程的新提交；
在本地创建和远程分支对应的分支，使用git checkout -b branch-name origin/branch-name，本地和远程分支的名称最好一致；
建立本地分支和远程分支的关联，使用git branch --set-upstream branch-name origin/branch-name；
从远程抓取分支，使用git pull，如果有冲突，要先处理冲突。</p>
<h3 id="10-工作中实践补充">10. 工作中实践补充</h3>
<pre><code class="language-text">//远程分支版本回退
	
如果你的错误提交已经推送到自己的远程分支了，那么就需要回滚远程分支了。 
首先要回退本地分支：
git reflog 查出git提交记录，然后找到需要回滚到的commitId，执行回滚命令：
git reset --hard commitId
紧接着强制推送到远程分支：
git push -f
</code></pre>
]]></content>
    </entry>
</feed>