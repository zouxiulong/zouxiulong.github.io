<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zouxiulong.github.io</id>
    <title>浮梦   的博客</title>
    <updated>2021-03-29T10:20:04.102Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zouxiulong.github.io"/>
    <link rel="self" href="https://zouxiulong.github.io/atom.xml"/>
    <subtitle>咸鱼一枚~</subtitle>
    <logo>https://zouxiulong.github.io/images/avatar.png</logo>
    <icon>https://zouxiulong.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 浮梦   的博客</rights>
    <entry>
        <title type="html"><![CDATA[ES6的Set和Map 以及WeakSet和WeakMap]]></title>
        <id>https://zouxiulong.github.io/post/es6-de-set-he-map-yi-ji-weakset-he-weakmap/</id>
        <link href="https://zouxiulong.github.io/post/es6-de-set-he-map-yi-ji-weakset-he-weakmap/">
        </link>
        <updated>2021-03-29T01:56:16.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1set">1.Set</h2>
<p>概念：ES6提供了新的数据结构Set，它类似数组，但是成员的值都是唯一的，没有重复的值。<br>
Set本身是一个构造函数，用来生成Set数据结构。</p>
<pre><code>// 用法
const set = new Set();
const set = new Set([1,2,1]);
</code></pre>
<h3 id="set实例的属性和方法">Set实例的属性和方法</h3>
<p>Set 结构的实例有以下属性。</p>
<ul>
<li>Set.prototype.constructor：构造函数，默认就是Set函数。</li>
<li>Set.prototype.size：返回Set实例的成员总数。</li>
</ul>
<p>Set 实例的方法分为两大类：操作方法（用于操作数据）和遍历方法（用于遍历成员）。下面先介绍四个操作方法。</p>
<ul>
<li>add(value)：添加某个值，返回Set结构本身。</li>
<li>delete(value)：删除某个值，返回一个布尔值，表示删除是否成功。</li>
<li>has(value)：返回一个布尔值，表示该值是否为Set的成员。</li>
<li>clear()：清除所有成员，没有返回值。</li>
</ul>
<h2 id="语法">语法</h2>
<h3 id="创建set">创建Set</h3>
<pre><code>const set = new Set();
console.log(set);  // Set(0) {}
</code></pre>
<h3 id="初始化set并创建">初始化Set并创建</h3>
<pre><code>const set = new Set(['🍉', '🍎', '🍍', '🍇']);
console.log(set);  // Set(4) {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍍&quot;, &quot;🍇&quot;}
</code></pre>
<h3 id="添加元素addvalue">添加元素：add(value)</h3>
<p>add(value):添加某个值，一个一个的添加，返回Set结构本身。</p>
<pre><code>// 使用add(value)方法将元素添加到中Set
const set = new Set();
set.add('🍅'); 
set.add('🥕'); 
set.add('🍉'); 
console.log(set); // Set(3) {&quot;🍅&quot;, &quot;🥕&quot;, &quot;🍉&quot;}
</code></pre>
<p>我们再添加两个元素</p>
<pre><code>set.add('🍉'); 
set.add('🍇'); 
console.log(set); // Set(4) {&quot;🍅&quot;, &quot;🥕&quot;, &quot;🍉&quot;, &quot;🍇&quot;}
</code></pre>
<p>输出发现只添加了🍇，而没添加🍉。没有添加🍉的因为是内部已经包含了🍉。</p>
<h3 id="判断该值是否为set的成员hasvalue">判断该值是否为Set的成员：has(value)</h3>
<p>判断是否存在🍅和🍉<br>
使用该has(value)方法在Set中查找元素。</p>
<pre><code>console.log('是否有🍉?', set.has('🍉')); // 有 🍉，所以返回true
console.log('是否有🥦?', set.has('🥦')); // 没有 🥦，所以返回false
</code></pre>
<h3 id="删除元素deletevalue">删除元素：delete(value)</h3>
<p>现在我们删除掉🥕，因为不喜欢吃它。</p>
<pre><code>set.delete('🥕');
console.log(set); // Set(3) {&quot;🍅&quot;, &quot;🍉&quot;, &quot;🍇&quot;}
</code></pre>
<h3 id="清除元素clearvalue">清除元素：clear(value)</h3>
<p>clear() 方法用于删除所有元素。</p>
<pre><code>set.clear();
console.log('清空完成了:', set);
</code></pre>
<h2 id="遍历操作用于遍历成员">遍历操作（用于遍历成员）</h2>
<p>Set结构的实例有四个遍历方法，可以用于遍历成员。</p>
<ul>
<li>keys():返回键名的遍历器</li>
<li>values():返回键值的遍历器</li>
<li>entries()：返回键值对的遍历器</li>
<li>forEach():使用回调函数遍历每个成员</li>
</ul>
<h3 id="keysvalues">keys()，values()</h3>
<p>由于Set结构没有键名，只有键值(或者说键名和键值是同一个值)，所以keys方法和values方法的行为完全一致。</p>
<pre><code>const set = new Set(['🍉', '🍎', '🍍', '🍇']);
console.log(set.keys());  // SetIterator {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
console.log(set.values());  // SetIterator {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
</code></pre>
<h3 id="entries">entries()</h3>
<p>entries方法返回的遍历器，同时包括键名和键值，所以每次输出一个数组，它的两个成员完全相等。</p>
<pre><code>console.log(set.entries());  // SetIterator {&quot;🍉&quot; =&gt; &quot;🍉&quot;, &quot;🍎&quot; =&gt; &quot;🍎&quot;, &quot;🍈&quot; =&gt; &quot;🍈&quot;, &quot;🍏&quot; =&gt; &quot;🍏&quot;}
</code></pre>
<h3 id="foreach">forEach()</h3>
<p>Set结构的实例的forEach方法，对每个成员执行某种操作，没有返回值。</p>
<pre><code>set.forEach((value,key, set) =&gt; console.log(value, key, set))
// 打印
// 🍉 🍉 Set(4) {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
// 🍎 🍎 Set(4) {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
// 🍈 🍈 Set(4) {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
// 🍏 🍏 Set(4) {&quot;🍉&quot;, &quot;🍎&quot;, &quot;🍈&quot;, &quot;🍏&quot;}
</code></pre>
<p>上面代码说明，forEach方法的参数就是一个处理函数。该函数的参数依次为键值、键名、集合本身。</p>
<h3 id="使用for-of">使用for-of</h3>
<p>遍历Array可以采用下标循环，遍历Map和Set就无法使用下标。为了统一集合类型，ES6标准引入了新的<br>
iterable类型，Array、Map和Set都属于iterable类型。具有iterable类型的集合可以通过新的for … of循环来遍历。</p>
<p>Set结构的实例默认可遍历，它的默认遍历器生成函数就是它的values方法。这意味着，可以省略values方法，直接用for...of循环遍历Set.</p>
<pre><code>for(const value of set) {
   console.log(value);
 }
</code></pre>
<h2 id="set应用">Set应用</h2>
<ol>
<li>数组去重</li>
</ol>
<pre><code>let set = ['🍉', '🍎', '🍍', '🍇', '🍎', '🍉']
const newSet = [...new Set(set)]
const newSet = Array.from(new Set(set))
</code></pre>
<p>注：将Set转为数组：Array.from 方法可以将 Set 数据结构转为数组</p>
<ol start="2">
<li>union（并集后集合）</li>
</ol>
<ul>
<li>Union 例如。{1，2，3}和{3，4，5}的并集后的集合是：{1,2,3,4,5}。</li>
</ul>
<pre><code>const set1 = new Set([1, 2, 3]);
const set2 = new Set([3, 4, 5]);
// Union
console.log('Union:', new Set([...set1, ...set2])); // Union: Set(5) {1, 2, 3, 4, 5}
</code></pre>
<ol start="3">
<li>intersection（交集）</li>
</ol>
<ul>
<li>Intersection 在集合A和B中，表示为A∩B的是集合中所有同时属于A和B的对象的集合。例如，{1，2，3}和{3，4，5}的交集是设置{3}。（可以理解为共有属性）</li>
</ul>
<pre><code>const set1 = new Set([1, 2, 3]);
const set2 = new Set([3, 4, 5]);
// Union
console.log('Union:', new Set([...set1, ...set2])); // Union: Set(5) {1, 2, 3, 4, 5}
</code></pre>
<ol start="4">
<li>diference，superset，subset</li>
</ol>
</br>
</br>
## 2.WeakSet
概念：WeakSet结构与Set类似，也是不重复的值的集合。但是，它与Set有两个区别。
</br>
WeakSet的成员只能是对象，而不能是其他类型的值。
```
let ws = new WeakSet()
        ws.add(1);
//  报错 弱集中使用的值无效
VM520:2 Uncaught TypeError: Invalid value used in weak set
    at WeakSet.add (<anonymous>)
    at <anonymous>:2:12
```
上面代码试图向WeakSet添加一个数值，因为WeakSet只能放置对象，所以报错了！
<p>其次，WeakSet 中的对象都是弱引用，即垃圾回收机制不考虑 WeakSet 对该对象的引用，也就是说，如果其他对象都不再引用该对象，那么垃圾回收机制会自动回收该对象所占用的内存，不考虑该对象还存在于 WeakSet 之中。</p>
<p>这是因为垃圾回收机制依赖引用计数，如果一个值的引用次数不为0，垃圾回收机制就不会释放这块内存。结束使用该值之后，有时会忘记取消引用，导致内存无法释放，进而可能会引发内存泄漏。WeakSet 里面的引用，都不计入垃圾回收机制，所以就不存在这个问题。因此，WeakSet 适合临时存放一组对象，以及存放跟对象绑定的信息。只要这些对象在外部消失，它在 WeakSet 里面的引用就会自动消失。</p>
<p>由于上面这个特点，WeakSet 的成员是不适合引用的，因为它会随时消失。另外，由于 WeakSet 内部有多少个成员，取决于垃圾回收机制有没有运行，运行前后很可能成员个数是不一样的，而垃圾回收机制何时运行是不可预测的，因此 ES6 规定 <strong>WeakSet 不可遍历</strong>。</p>
<h2 id="语法-2">语法</h2>
<pre><code>const ws = new WeakSet();
</code></pre>
<p>WeakSet是一个构造函数，可以使用new命令，创建WeakSet数据结构。</p>
<pre><code>const a = [[1,2],[3,4]];
const ws = new WeakSet(a);
//WeakSet {[1,2],[3,4]}
</code></pre>
<p>上面代码中，a是一个数组，它有两个成员，也都是数组（类似于二维数组，直接传数组会报错）。将a作为 WeakSet 构造函数的参数，a的成员会自动成为 WeakSet 的成员。</p>
<p>WeakSe有以下三个方法。</p>
<ul>
<li>add(value)：向WeakSet实例添加一个新成员。</li>
<li>delete(value)：清除WeakSet实例的指定成员。</li>
<li>has(value)：返回一个布尔值，标识某个值是否存在WeakSet实例之中。</li>
</ul>
<pre><code>const ws = new WeakSet();
const obj = {};
const foo = {};
const fun = function() {}

ws.add(window);
ws.add(obj);
ws.add(fun);

ws.has(window); // true
ws.has(obj); // true
ws.has(fun); // true

ws.delete(window);
ws.delete(obj);
ws.delete(fun);
</code></pre>
<p>WeakSet没有size属性，没有办法遍历它的成员。<br>
WeakSet 不能遍历，是因为成员都是弱引用，随时可能消失，遍历机制无法保证成员的存在，很可能刚刚遍历结束，成员就取不到了。WeakSet 的一个用处，是储存 DOM 节点，而不用担心这些节点从文档移除时，会引发内存泄漏。</p>
<h2 id="3map">3.Map</h2>
<p>概念：Map是ES6引入的一种类似Object的新的数据结构，Map可以理解为是Object的超集，打破了以传统键值对形式定义对象，对象的key不再局限于字符串，也可以是Object。可以更加全面的描述对象的属性。</p>
<p>JavaScript的对象(Object),本质上是键值对的集合，但是传统上只能用字符串当做键，这给它的使用带来了很大的限制。</p>
<pre><code>let obj = {
    type: &quot;🍎&quot;
}
let data = {}
data[obj] = '🍎'
console.log(data); //  {[object Object]: &quot;🍎&quot;}

</code></pre>
<p>上面代码是将一个对象作为data的键，但是因为对象的键名只能是字符串，所以就把对象obj自动转为字符串[object Object].</p>
<p>为了解决这个问题，ES6提供了Map数据结构，它类似于对象，也是键值对的集合，但是'键'范围不限于字符串，各种类型的值(包括对象)都可以当做键。也就是说，Object结构提供了'字符串-值'的对应，Map结构提供了'值-值'的对应。</p>
<h3 id="map实例的属性和方法">Map实例的属性和方法</h3>
<p>Map结构的实例有以下属性和操作方法：</p>
<ul>
<li>size属性：size属性返回Map结构的成员总数。</li>
<li>set(key, value)：设置键名key对应的键值为value，返回Map结构本身。</li>
<li>get(key)：读取key对应的键值，如果找不到key，返回undefined。</li>
<li>delete(key)：删除某个键，返回true，如果删除失败，返回false。</li>
<li>has(key)：返回一个布尔值，标识某个键是否在当前Map对象值中。</li>
<li>clear()：清除所有成员，没有返回值。</li>
</ul>
<h3 id="size属性">size属性</h3>
<p>size属性返回Map结构的成员总数</p>
<pre><code>const map = new Map();
map.set('1',true);
map.set('2',false);
map.size // 2
</code></pre>
<h3 id="添加元素setkeyvalue">添加元素：set(key,value)</h3>
<p>set方法设置键名key对应的键值为value，然后返回整个Map结构，所以可以采用链式结构，如果key已经有值，则键值会被更新，否则就新生成该键。</p>
<pre><code>const m = new Map();
let obj = {
    type: &quot;🍎&quot;
}
let fun = function() {}

m.set('abc',666);  // 键是字符串
m.set(222,'abc'); // 键是数值
m.set(fun,'我是function'); // 键是 function

m.set(undefined,'我是undefined').set(obj, '我是苹果'); // 链式编程
</code></pre>
</br>
</br>
### 获取元素：get(key)
get方法读取key对应的键值，如果找不到key，返回undefined。
```
const m = new Map();
const fun = function(){
    console.log('⭐⭐⭐');
};
m.set(fun,'我是值'); // 键是函数
m.get(fun) // 我是值
```
</br>
</br>
### 判断该值是否为Map的成员has(key)
has方法返回一个布尔值，标识某个键是否在当前Map对象值中。
```
const m = new Map();
const fun = function(){
    console.log('⭐⭐⭐');
};
m.set(fun,'我是值'); // 键是函数
<p>m.has(fun); // true<br>
m.has('我是值'); // false</p>
<pre><code>&lt;/br&gt;
&lt;/br&gt;
### 删除元素：delete(key)
delete方法删除某个键，返回true，如果删除失败，返回false。
</code></pre>
<p>const m = new Map();<br>
const fun = function(){<br>
console.log('⭐⭐⭐');<br>
};<br>
m.set(fun,'我是值'); // 键是函数<br>
m.has(fun); // true</p>
<p>m.delete(fun); // true<br>
m.has(fun); // false</p>
<pre><code>&lt;/br&gt;
&lt;/br&gt;
### 清除元素：clear()
clear方法清除所有成员，没有返回值。
</code></pre>
<p>const m = new Map();<br>
m.set(1,'我是值'); // 键是函数<br>
m.set(2,'我是值'); // 键是函数</p>
<p>m.size // 2<br>
m.clear()<br>
m.size // 0</p>
<pre><code>&lt;/br&gt;
## 遍历方法
Map常用遍历方法：
- Keys()：将Map中包含的所有键名以迭代器形式返回
- values()：将Map中包含的所有数值以迭代器形式返回
- entries()：返回所有成员的迭代器
- forEach()：遍历Map的所有成员
</code></pre>
<p>const map = new Map([<br>
['F', 'no'],<br>
['T',  'yes'],<br>
]);</p>
<p>for (let key of map.keys()) {<br>
console.log(key);<br>
}<br>
// &quot;F&quot;<br>
// &quot;T&quot;</p>
<p>for (let value of map.values()) {<br>
console.log(value);<br>
}<br>
// &quot;no&quot;<br>
// &quot;yes&quot;</p>
<p>for (let item of map.entries()) {<br>
console.log(item[0], item[1]);<br>
}<br>
// &quot;F&quot; &quot;no&quot;<br>
// &quot;T&quot; &quot;yes&quot;</p>
<p>// 或者<br>
for (let [key, value] of map.entries()) {<br>
console.log(key, value);<br>
}<br>
// &quot;F&quot; &quot;no&quot;<br>
// &quot;T&quot; &quot;yes&quot;</p>
<p>// 等同于使用map.entries()<br>
// Map结构的默认遍历器使用的是entries方法<br>
for (let [key, value] of map) {<br>
console.log(key, value);<br>
}<br>
// &quot;F&quot; &quot;no&quot;<br>
// &quot;T&quot; &quot;yes&quot;</p>
<pre><code>&lt;/br&gt;
## WeakMap
概念：WeakMap结构与Map结构类似,也是用于生成键值对的集合。
</code></pre>
<p>// WeakMap 可以使用 set 方法添加成员<br>
const wm = new WeakMap();<br>
const key = {foo: 1};<br>
wm.set(key, 2);<br>
wm.get(key) // 2</p>
<p>// WeakMap 也可以接受一个数组，<br>
// 作为构造函数的参数<br>
const k1 = [1, 2, 3];<br>
const k2 = [4, 5, 6];<br>
const wm2 = new WeakMap([[k1, 'foo'], [k2, '我是值']]);<br>
wm2.get(k2) // &quot;我是值&quot;</p>
<pre><code>&lt;/br&gt;
&lt;/br&gt;
WeakMap语法
WeakMap 与 Map 在 API 上的区别主要是两个，一是没有遍历操作（即没有key()、values()和entries()方法），也没有size属性。因为没有办法列出所有键名，某个键名是否存在完全不可预测，跟垃圾回收机制是否运行相关。这一刻可以取到键名，下一刻垃圾回收机制突然运行了，这个键名就没了，为了防止出现不确定性，就统一规定不能取到键名。二是无法清空，即不支持clear方法。因此，WeakMap只有四个方法可用：get()、set()、has()、delete()。

分别是：
- set(key, value)：设置键名key对应的键值为value，返回WeakMap结构本身。
- get(key)：读取key对应的键值，如果找不到key，返回undefined。
- delete(key)：删除某个键，返回true，如果删除失败，返回false。
- has(key)：返回一个布尔值，标识某个键是否在当前WeakMap对象值中。
&lt;/br&gt;
&lt;/br&gt;
WeakMap与Map的区别有两点.
首先,WeakMap只接受对象作为键名,不接受其他类型的值作为键名.
&lt;/br&gt;
&lt;/br&gt;
其次，WeakMap的键名所指向的对象，不计入垃圾回收机制。
WeakMap 就是为了解决这个问题而诞生的，它的键名所引用的对象都是弱引用，即垃圾回收机制不将该引用考虑在内。因此，只要所引用的对象的其他引用都被清除，垃圾回收机制就会释放该对象所占用的内存。也就是说，一旦不再需要，WeakMap 里面的键名对象和所对应的键值对会自动消失，不用手动删除引用。

基本上，如果你要往对象上添加数据，又不想干扰垃圾回收机制，就可以使用 WeakMap。一个典型应用场景是，在网页的 DOM 元素上添加数据，就可以使用WeakMap结构。当该 DOM 元素被清除，其所对应的WeakMap记录就会自动被移除。
&lt;/br&gt;
</code></pre>
<p>const wm = new WeakMap();</p>
<p>const element = document.querySelector(&quot;#add&quot;);<br>
wm.set(element, 'addBtn');<br>
wm.get(element) // &quot;addBtn&quot;</p>
<pre><code>上面代码中，先新建一个 WeakMap 实例。然后，将一个 DOM 节点作为键名存入该实例，并将一些附加信息作为键值，一起存放在 WeakMap 里面。这时，WeakMap 里面对element的引用就是弱引用，不会被计入垃圾回收机制。

也就是说，上面的 DOM 节点对象的引用计数是1，而不是2。这时，一旦消除对该节点的引用，它占用的内存就会被垃圾回收机制释放。WeakMap 保存的这个键值对，也会自动消失。

注意，WeakMap 弱引用的只是键名，而不是键值。键值依然是正常引用。

## WeakMap的实例

get、set、has、delete


WeakMap 的例子很难演示，因为无法观察它里面的引用会自动消失。此时，其他引用都解除了，已经没有引用指向 WeakMap 的键名了，导致无法证实那个键名是不是存在。

如果引用所指向的值占用特别多的内存，那其实我们可以通过判断内存的大小来判断是否有造成了内存泄漏











</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二分查找]]></title>
        <id>https://zouxiulong.github.io/post/suan-fa-er-fen-cha-zhao/</id>
        <link href="https://zouxiulong.github.io/post/suan-fa-er-fen-cha-zhao/">
        </link>
        <updated>2021-03-24T03:49:02.000Z</updated>
        <summary type="html"><![CDATA[<p>二分查找也称为折半查找，是指在有序的数组里找出指定的值，返回该值在数组中的索引。</p>
]]></summary>
        <content type="html"><![CDATA[<p>二分查找也称为折半查找，是指在有序的数组里找出指定的值，返回该值在数组中的索引。</p>
<!-- more -->
<h3 id="二分查找">二分查找</h3>
<blockquote>
<p>二分查找也称折半查找（Binary Search），是一种在有序数组中查找某一特定元素的搜索算法。我们可以从定义可知，运用二分搜索的前提是数组必须是有序的，这里需要注意的是，我们的输入不一定是数组，也可以是数组中某一区间的起始位置和终止位置。</p>
</blockquote>
<h4 id="注二分查找必须在有序数组中才有效无序的数组不能实现查找功能-比如在105678920中查找10中间索引位置的值为7比较得出7比10小因而应该在右子数组中查找实际上不可能找到10">注：二分查找必须在有序数组中才有效，无序的数组不能实现查找功能。比如：在[10,5,6,7,8,9,20]中查找10，中间索引位置的值为7，比较得出7比10小，因而应该在右子数组中查找，实际上不可能找到10；</h4>
</br>
<p>我们通过下面的栗子来了解二分查找具体是怎么实现的，首先我们先定义一个数组，查询元素 8 的索引。</p>
<pre><code>let array = [1,3,4,5,6,8,12,14,16]  // 查找元素为8的索引
</code></pre>
</br>
1. 我们先定义两个指针分别指向数组的头部和尾部，这是我们在整个数组中查找的范围，当我们在数组某一区间进行查询时，可以输入数组，起始位置，终止位置进行查询。如下图</br>
<figure data-type="image" tabindex="1"><img src="https://zouxiulong.github.io/post-images/1616566740872.png" alt="" loading="lazy"></figure>
<p></br></br><br>
2. 我们需要找出mid，也就是 mid =（left + right）/ 2，通过首末索引相加来找到中间索引，通过中间索引判断目标元素所在的区域，注意（left + right）/ 2 的结果可能会出现有小数点的情况，我们需要对它进行向下取整，也就是Math.floor( (left + right) / 2 )</br></p>
<figure data-type="image" tabindex="2"><img src="https://zouxiulong.github.io/post-images/1616567136663.png" alt="" loading="lazy"></figure>
<p></br></br><br>
3. 此时我们的 mid = 4，array[mid] = 6 &lt; target,那么我们需要移动我们的 left 指针，让left = mid + 1，下次则可以在新的 left 和 right 区间内搜索目标值，下图为移动前和移动后</br></p>
<figure data-type="image" tabindex="3"><img src="https://zouxiulong.github.io/post-images/1616568061183.png" alt="" loading="lazy"></figure>
<p></br></br><br>
4. 我们需要在 left 和 right 之间重新计算 mid 值，此时的 mid = Math.floor( (8 + 5) / 2 ) = 6 然后将 array[mid] 与 target 继续比较，进而决定下次移动left 指针还是 right 指针，见下图</br></p>
<figure data-type="image" tabindex="4"><img src="https://zouxiulong.github.io/post-images/1616568157637.png" alt="" loading="lazy"></figure>
<p></br></br><br>
5.我们发现 array[mid] &gt; target，则需要移动我们的 right 指针， 则 right = mid - 1；则移动过后我们的 left 和 right 会重合，这里是我们的一个重点大家需要注意一下，后面会对此做详细叙述。</br></p>
<figure data-type="image" tabindex="5"><img src="https://zouxiulong.github.io/post-images/1616568610157.png" alt="" loading="lazy"></figure>
<p></br></br><br>
6.我们需要在 left 和 right 之间继续计算 mid 值，则 mid = Math.floor( (5 +5) / 2 ) = 5 ，见下图，此时我们将 array[mid] 和 target 比较，则发现两值相等，返回 mid 即可，如果不相等则跳出循环，返回 -1。</br></p>
<p><img src="https://zouxiulong.github.io/post-images/1616568708220.png" alt="" loading="lazy"><br>
</br></br></p>
<h3 id="二分查找的执行过程如下">二分查找的执行过程如下</h3>
<ol>
<li>首先要拿到有序数组的首末索引</li>
<li>通过首末索引相加来找到中间索引，通过中间索引判断目标元素所在的区域</li>
<li>如果碰巧mid对应的数就是目标，那就直接返回该索引</li>
<li>如果mid索引对应的元素大于target，那么证明target，在以mid分割的数组的前半部分，所以要让末尾索引变成 mid - 1，相反就让首索引变成 mid + 1</li>
<li>查找结束条件，1.就是成功找到目标元素，然后返回相对应的索引，2.没有查找到目标元素，返回-1</li>
</ol>
<p>下面是我在网上找的二分查找动画演示，大家只要关注上面那个即可：</br></br></p>
<figure data-type="image" tabindex="6"><img src="https://zouxiulong.github.io/post-images/1616568850013.webp" alt="" loading="lazy"></figure>
<p></br></br><br>
下面我们来看一下二分查找的代码</p>
<pre><code>//array:数组  target:查找的元素
function search(array, target) {
    // 初始索引开始位置和结束位置
    let left = 0,
        right = array.length - 1
    while(left &lt;= right) {
        // 取中间的索引
        let mid = Math.floor((left + right) / 2)
        if(array[mid] === target) {
            // 如果找到则直接返回
            return mid
        }else if(array[mid] &gt; target) {
            // 如果target是小于数组中间索引的值则将索引结束位置设置为中间索引-1
            right = mid - 1
        }else {
            // 如果target是大雨数组中间索引的值则将索引结束位置设置为中间索引+1
            left = mid + 1
        }
    }
    // 如果在循环内没有找到查找的target (left &lt;= right) 的情况则返回-1
    return -1
}
let array = [1,3,4,5,6,8,12,14,16]
console.log(search(array, 2)); // -1
console.log(search(array, 3)); // 1
</code></pre>
<p>递归版本:</p>
<pre><code>// array:数组 target:查找的元素 left:开始索引 right:结束索引
function search(array, target, left, right) {
    // 首先判断当前起始索引是否大于结束索引,如果大于说明没有找到元素返回-1
    if(left &gt; right)  {
        return -1
    }
    // 如果手动调用不写left和right参数会当做第一次运行默认值
    // 三元表达式:如果不写right参数则为undefined说明第一次调用所以结束索引为array.length-1
    // 如果是递归调用则使用传进来的参数right值, 
    // left同理right
    var left = left == undefined ? 0 : left
    var right = right == undefined ? array.length - 1 : right 
    var mid = Math.floor((left + right) / 2)
    
    if(array[mid] === target) {
        return mid
    }else if(array[mid] &gt; target) {
        right = mid - 1
        return search(array, target, left, right)
    }else {
        left = mid + 1
        return search(array, target, left, right)
    }
}
console.log(search(array, 2)); // -1
console.log(search(array, 3)); // 1
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[快速排序]]></title>
        <id>https://zouxiulong.github.io/post/js-suan-fa-kuai-pai/</id>
        <link href="https://zouxiulong.github.io/post/js-suan-fa-kuai-pai/">
        </link>
        <updated>2021-03-23T09:44:41.000Z</updated>
        <content type="html"><![CDATA[<p>个人认为，快速排序的基本思想就是：<br>
将需要排序的数切分成两段，一段小，一段大，通过递归不断切分，从而完成了排序。</p>
<p>具体步骤如下：</p>
<blockquote>
<ol>
<li>从需要排序的数组中随便选择一个数，作为中间数 ，保存到一个变量 centerValue 中。不过一般为了方便，可直接选择第一个数或者中间那个数为中间数。</li>
<li>从需要排序的数组里删除这个中间数。</li>
<li>定义两个新数组，左数组 leftArray 和右数组 rightArray 。</li>
<li>遍历数组，依次与中间数 centerValue 比较大小。<br>
• 比 centerValue 小的放左数组 leftArray ；<br>
• 比 centerValue 大的放右数组 rightArray ；</li>
<li>使用递归的方式，按以上规则继续对左数组leftArray 和右数组 rightArray 进行划分。</li>
<li>返回排序结果。</li>
<li>为防止递归陷入死循环，需要在函数开头添加一个判断条件，即：如果数组的长度小于等于1，则将这个数组return回去。</li>
</ol>
</blockquote>
<pre><code>function quickSort(array) {
    // 为防止递归陷入死循环
    if(array.length &lt;= 1) {
        return array
    }
    let leftArray = [],
        rightArray = [],
        centerValue = array.shift()
    
    array.forEach(item =&gt; {
        if(item &lt; centerValue) {
            // 如果小于中间值 则push到leftArray里面
            leftArray.push(item)
        }else {
            // 否则push到rightArray里面
            rightArray.push(item)
        }
    });
    
    let leftArraySortRes = quickSort(leftArray)
    let rightArraySortRes = quickSort(rightArray)
    return leftArraySortRes.concat(centerValue, rightArrSortRes)
    // 等同于上面的代码
    // return quickSort(leftArray).concat(centerValue, quickSort(rightArray))
}

let newArray = quickSort([52,45,4,2,64,21,652,15,1,0,-54,-1,58,7,9])
console.log(newArray); // [-54, -1, 0, 1, 2, 4, 7, 9, 15, 21, 45, 52, 58, 64, 652]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[手写一个call、apply、bind]]></title>
        <id>https://zouxiulong.github.io/post/shouxie-call-apply-bind/</id>
        <link href="https://zouxiulong.github.io/post/shouxie-call-apply-bind/">
        </link>
        <updated>2021-03-12T06:04:11.000Z</updated>
        <summary type="html"><![CDATA[<p>为什么要手写代码，不仅仅是为了面试，也是为了帮助我们更好的理解代码逻辑，深入其中原理最好的方法。</p>
]]></summary>
        <content type="html"><![CDATA[<p>为什么要手写代码，不仅仅是为了面试，也是为了帮助我们更好的理解代码逻辑，深入其中原理最好的方法。</p>
<!-- more -->
<p>call 和 apply 都是为了解决改变 this 的指向。作⽤都是相同的，只是传参的⽅式 不同。 除了第⼀个参数外， call 可以接收⼀个参数列表， apply 只接受⼀个参数数组。bind 和其他两个⽅法作⽤也是⼀致的，只是该⽅法会返回⼀个函数。</p>
<h2 id="它们有什么不同">它们有什么不同？</h2>
<blockquote>
<p>它们接收的第一个参数都是函数上下文也就是要绑定的this指向。<br>
apply的第二个参数是一个参数数组，call和bind的第二个及之后的参数作为函数实参按顺序传入。<br>
bind绑定完this的指向后会返回一个新的函数体，不会被立即调用，其他两个会立即调用。</p>
</blockquote>
<p>话不多说，直接上代码</p>
<pre><code>// 公共部分代码
function getValue(from, to){
    console.log( this.name + '年龄' + this.age, '来自' + from + '去往' + to );
}
var data = {
    name: '小邹',
    age: '18'
}
</code></pre>
<h2 id="实现call方法">实现call方法</h2>
<pre><code>Function.prototype.myCall = function(context) {
    //this必须是一个函数
    if(typeof this !== 'function') {
       throw new Error('type Error')
    }
    context = context || window  //context赋值为传入来的对象
    context.fn = this // 一开始的this是指向要调用的那个函数，这里先将这个函数归纳到context对象里面
    let args = Array.from(arguments).slice(1) // 把传入的参数截取出来
    context.fn(...args) //调用这个函数
    delete context.fn
}
</code></pre>
<h2 id="实现apply方法">实现apply方法</h2>
<pre><code>Function.prototype.myApply = function(context) {
    if(typeof this !== 'function') throw new Error('type Error')
    context = context || window
    let args = [...arguments].slice(1)
    context.fn = this
    context.fn(...args[0]) //这里传入的是一个数组，需要将数组每一项传进去
    delete context.fn
}
</code></pre>
<h2 id="实现bind方法">实现bind方法</h2>
<pre><code>Function.prototype.myBind = function(context) {
    if(typeof this !== 'function') throw new Error('type Error')
    context = context || window
    let fn = this // 保存this，这里的this指的是getValue方法
    let args = [...arguments].slice(1)
    return function() {
        let rets = args.concat(Array.from(arguments))
        fn.myApply(context, rets)
    }
}
</code></pre>
<h2 id="测试代码">测试代码</h2>
<pre><code>getValue.myCall(data, '九江', '上海') // 小邹年龄18 来自九江去往上海
getValue.myApply(data, ['九江', '上海']) // 小邹年龄18 来自九江去往上海
getValue.myBind(data, '九江', '上海')(&quot;浦东&quot;) // 小邹年龄18 来自九江去往上海浦东
</code></pre>
<p>收工，你学废了吗？</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[广度优先遍历（BFS）和深度优先遍历（DFS）]]></title>
        <id>https://zouxiulong.github.io/post/js-BFS-DFS/</id>
        <link href="https://zouxiulong.github.io/post/js-BFS-DFS/">
        </link>
        <updated>2021-02-26T04:33:15.000Z</updated>
        <content type="html"><![CDATA[<p>先用一个DOM树来进行说明</p>
<pre><code>&lt;div class=&quot;parent&quot;&gt;
        &lt;div class=&quot;child-1&quot;&gt;
            &lt;div class=&quot;child-1-1&quot;&gt;
                &lt;div class=&quot;child-1-1-1&quot;&gt;
                    &lt;div class=&quot;child-1-1-1-1&quot;&gt;a&lt;/div&gt;
                &lt;/div&gt;
            &lt;/div&gt;
            &lt;div class=&quot;child-1-2&quot;&gt;
                &lt;div class=&quot;child-1-2-1&quot;&gt;b&lt;/div&gt;
            &lt;/div&gt;
            &lt;div class=&quot;child-1-3&quot;&gt;c&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;child-2&quot;&gt;
            &lt;div class=&quot;child-2-1&quot;&gt;d&lt;/div&gt;
            &lt;div class=&quot;child-2-2&quot;&gt;e&lt;/div&gt;
        &lt;/div&gt;
        &lt;div class=&quot;child-3&quot;&gt;
            &lt;div class=&quot;child-3-1&quot;&gt;f&lt;/div&gt;
        &lt;/div&gt;
    &lt;/div&gt;
</code></pre>
<h2 id="深度优先遍历">深度优先遍历</h2>
<blockquote>
<p>该方法是以纵向的维度dom进行遍历，从dom根节点开始，一直沿着最近的子节点遍历下去，直到它的所有子节点遍历完毕后再沿着它的兄弟节点进行下去，知道所有节点遍历完成。</p>
</blockquote>
<p>如图所示：<br>
<img src="https://zouxiulong.github.io/post-images/1614661433049.jpg" alt="" loading="lazy"></p>
<p>js实现该算法（递归）：</p>
<pre><code>function deepNode(node, nodeList = []) {
    // 判断node是否存在
    if(node) {
        nodeList.push(node)
        for (let i = 0; i &lt; node.children.length; i++) {
            // 每次遍历需要将 子节点 和 已经存储 的子节点 传进去
            deepNode(node.children[i], nodeList)
        }
    }
    return nodeList
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM, []));
</code></pre>
<p>打印结果：<br>
<img src="https://zouxiulong.github.io/post-images/1614662208602.jpg" alt="" loading="lazy"></p>
<p>js实现该算法（非递归）：</p>
<pre><code>function deepNode(node) {
    var nodeList = [];  
    if (node) {  
        var queue = [];  
        queue.push(node); 
        while (queue.length != 0) {  
            var childrenItem = queue.pop();  // 取数据最后一个节点
            nodeList.push(childrenItem);  
            var childrenList = childrenItem.children;  
            for (var i = childrenList.length-1; i &gt;= 0; i--)  {
                queue.push(childrenList[i]);  // 有children，向后push数据
            }
        }  
    }    
    return nodeList;  
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM));
</code></pre>
<p>打印结果：<br>
<img src="https://zouxiulong.github.io/post-images/1614668416846.jpg" alt="" loading="lazy"></p>
<h2 id="广度优先遍历">广度优先遍历</h2>
<blockquote>
<p>该方法是以横向的维度进行遍历，从一个节点开始，尝试访问尽可能靠近它的目标节点。本质上这种遍历在图上是逐层移动的，首先检查最靠近第一个节点的层，再逐渐向下移动到离起始节点最远的层（犹如爬楼，走完一层的楼梯，继续下一层的楼梯）</p>
</blockquote>
<p>如图所示：<br>
<img src="https://zouxiulong.github.io/post-images/1614663746263.jpg" alt="" loading="lazy"></p>
<p>js实现该算法：</p>
<pre><code>function deepNode(node) {
    let nodeList = []
    if(node != null) {
        let queue = [] // 遍历的集合
        queue.push(node)
        // 有值进行遍历，直到条件不满足
        while(queue.length != 0) {
            let item = queue.shift()
            nodeList.push(item)
            let children = item.children
            for (let i = 0; i &lt; children.length; i++) {
                queue.push(children[i])
            }
        }
    }
    return nodeList
}
let DOM = document.querySelector('.parent')
console.log(deepNode(DOM));
</code></pre>
<p>打印结果：<br>
<img src="https://zouxiulong.github.io/post-images/1614663133808.jpg" alt="" loading="lazy"></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[冒泡排序]]></title>
        <id>https://zouxiulong.github.io/post/js-maopao/</id>
        <link href="https://zouxiulong.github.io/post/js-maopao/">
        </link>
        <updated>2021-02-25T07:26:05.000Z</updated>
        <summary type="html"><![CDATA[<p>对数组进行 冒泡排序 算是比较简单的，冒泡排序也是容易理解的一种排序算法了，在面试的时候，很可能就会问到。</p>
]]></summary>
        <content type="html"><![CDATA[<p>对数组进行 冒泡排序 算是比较简单的，冒泡排序也是容易理解的一种排序算法了，在面试的时候，很可能就会问到。</p>
<!-- more -->
<h2 id="实现原理">实现原理</h2>
<blockquote>
<p>数组有 n 个数，比较每相邻的两个数，如果前者比后者大，则把两个数位置进行位置交换。一轮下来就可以选出最大的一个数放在最后面；经过 n 轮比较就完成了所有数的排序。<br>
如图所示：<br>
<img src="https://zouxiulong.github.io/post-images/1614670549266.webp" alt="" loading="lazy"></p>
</blockquote>
<p>话不多说，直接上代码</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
for(let i = 0; i &lt; arr.length - 1; i++ ) {
    // 如果前一个数 大于 后一个数则交换位置
    if(arr[i] &gt; arr[i+1]) {
        let max = arr[i] // 声明一个变量暂存数据
        arr[i] = arr[i+1]
        arr[i+1] = max
    }
}
console.log(arr)  // [0, 5, 6, 4, 9, 21, 10, 9, -6, 5, 15, -10, 27]  // 第一轮得出最大值
</code></pre>
<p>这里有一个难点，就是 <strong>i &lt; arr.length - 1</strong> ，是因为倒数第二轮进行比较的时候已经得出了最大值，所以 arr.length 可以进减1。</p>
<p>我们继续完善代码</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
// 遍历数组，次数就是arr.length - 1
for (let j = 0; j &lt; arr.length - 1; j++) {
    
    for(let i = 0; i &lt; arr.length - 1; i++ ) {
        console.log(arr[i] , arr[i+1]);
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i]
            arr[i] = arr[i+1]
            arr[i+1] = max
        }
    }
    
}
console.log(arr)  // [-10, -6, 0, 4, 5, 5, 6, 9, 9, 10, 15, 21, 27] 
</code></pre>
<p>我们能找到数组中最大的数，放到最后，这样重复 arr.length - 1 次，便可以实现数组按从小到大的顺序排好了。为什么是 arr.length - 1 次 ，因为倒数第二轮已经拍好了顺序，这里我们可以进行减1，减少不必要的循环。</p>
<p>虽然上面的代码已经实现冒泡排序了，但就像注释中提到的，内层 for 循环的次数写成，i &lt; arr.length - 1 ，是不是合适呢？<br>
我们想一下，当第一次，找到最大数，放到最后，那么下一次，遍历的时候，是不是就不能把最后一个数算上了呢？因为他就是最大的了，不会出现，前一个数比后一个数大，要交换位置的情况，所以内层 for 循环的次数，改成 i &lt; arr.length - 1 - j ，才合适，看下面的代码。</p>
<pre><code>let arr = [0,5,9,6,4,27,21,10,9,-6,5,15,-10]
for (let j = 0; j &lt; arr.length - 1; j++) {
    // 这里要根据外层for循环的 j，逐渐减少内层 for循环的次数
    for(let i = 0; i &lt; arr.length - 1 - j; i++ ) {
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i]
            arr[i] = arr[i+1]
            arr[i+1] = max
        }
    }
}
</code></pre>
<p>我们想下有种情况，当原数组是：arr = [1,2,4,3]<br>
在经过第一轮冒泡排序之后，数组就变成了：arr = [1,2,3,4]<br>
此时，数组已经排序完成了，但是按上面的代码来看，数组还会继续排序，所以我们加一个标志位，如果某次循环完后，没有任何两数进行交换，就将标志位 设置为 true，表示排序完成，这样我们就可以减少不必要的排序，提高性能。</p>
<p>终极代码：</p>
<pre><code>let arr = [1,2,4,3]
for (let j = 0; j &lt; arr.length - 1; j++) {
    // 声明一个变量，作为标志位
    let flag = true
    for(let i = 0; i &lt; arr.length - 1 - j; i++ ) {
        if(arr[i] &gt; arr[i+1]) {
            let max = arr[i] 
            arr[i] = arr[i+1]
            arr[i+1] = max
						flag = false
        }
    }
    if(flag) break
}
</code></pre>
<h2 id="总结">总结</h2>
<p>1、外层 for 循环控制循环次数<br>
2、内层 for 循环进行两数交换，找每次的最大数，排到最后<br>
3、设置一个标志位，减少不必要的循环</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[理解 v-model、v-bind、$attrs、 $listener 和 .sync 及其在跨组件数据双向绑定上的应用]]></title>
        <id>https://zouxiulong.github.io/post/li-jie-v-modelv-binddollarattrs-dollarlistener-he-sync-ji-qi-zai-kua-zu-jian-shu-ju-shuang-xiang-bang-ding-shang-de-ying-yong/</id>
        <link href="https://zouxiulong.github.io/post/li-jie-v-modelv-binddollarattrs-dollarlistener-he-sync-ji-qi-zai-kua-zu-jian-shu-ju-shuang-xiang-bang-ding-shang-de-ying-yong/">
        </link>
        <updated>2020-12-21T02:14:40.000Z</updated>
        <content type="html"><![CDATA[<h2 id="1-v-model-的本质">1. v-model 的本质</h2>
<pre><code class="language-text">&lt;MyList v-model=&quot;lovingVue&quot;&gt;&lt;/MyList&gt;
 
&lt;!-- 其实是以下的语法糖 --&gt;
&lt;MyList :value=&quot;lovingVue&quot; @input=&quot;(data) =&gt; lovingVue = data&quot;&gt;&lt;/MyList&gt;
</code></pre>
<p>没耐心理解代码的话，直接看下面的文字：<br>
v-model 的本质是：父组件给子组件传一个名为 value 的prop，然后对子组件挂载一个名为 input 的事件监听。<br>
当子组件手动 emit 这个input 事件时，携带的载荷自动赋值到v-model后绑的父组件变量上。<br>
（所以其实不是自动双向绑定，还是需要手动emit input事件的。）</p>
<h2 id="2-v-bind的本质">2. v-bind的本质</h2>
<p>本质是 批量传入props</p>
<p>例如：</p>
<pre><code class="language-text">&lt;Component v-bind=&quot;{a: foo, b: bar, c: baz}&quot;&gt;

&lt;!-- 相当于 --&gt;
&lt;Component  :a=foo :b=bar :c=baz&gt;
</code></pre>
<h2 id="3-attrs的本质">3. $attrs的本质</h2>
<p>官方解释：<br>
包含了父作用域中不作为 prop 被识别 (且获取) 的特性绑定 (class 和 style 除外)。当一个组件没有声明任何 prop 时，这里会包含所有父作用域的绑定 (class 和 style 除外)，并且可以通过 v-bind=&quot;$attrs&quot; 传入内部组件——在创建高级别的组件时非常有用。</p>
<p>我的理解：<br>
接收除了props声明外的所有绑定属性（class、style除外）</p>
<p>父组件以形如:foo=&quot;xxx&quot;或者v-bind=&quot;{age:12}&quot;传给子组件的属性，但凡没有被子组件的props接收的，都会被扔到子组件的$attrs里去。</p>
<h2 id="4-listeners的本质">4. $listeners的本质</h2>
<p>官方解释：<br>
包含了父作用域中的 (不含 .native 修饰器的) v-on 事件监听器。它可以通过 v-on=&quot;$listeners&quot; 传入内部组件——在创建更高层次的组件时非常有用。</p>
<p>我的理解：<br>
接收除了带有.native事件修饰符的所有事件监听器</p>
<p>父组件以 @eventName=&quot;fn&quot; 或者 v-on:eventName=&quot;fn&quot; 对子组件挂载事件监听。对子组件而言，父组件监听的事件都放在$listeners里。</p>
<p>如果子组件对后代组件使用v-on=&quot;$listeners&quot;，相当于对后代组件批量挂载了父组件对自己的事件监听。因此后代组件的emit会触发父组件的事件方法。</p>
<p>-----------------------------------------------------------------------------------------------------------------</br><br>
（以下关于 vue 事件内部实现，前后文无关，可跳过。）</p>
<p>打印 $listeners，可以看到如下结构。是一个 事件名+回调函数（数组） 构成的对象。</p>
<p><code>图中是后代组件的 $listeners，发现当在子组件中，如果对后代组件 v-on=&quot;$listeners&quot; 的同时，新增同名监听如@on-page-change='xxx'，那么父组件的回调、子组件的回调，都会被加进后代组件的回调函数栈中。</code></p>
<figure data-type="image" tabindex="1"><img src="https://zouxiulong.github.io/post-images/1610504445667.png" alt="" loading="lazy"></figure>
<p>这说明，vue on-emit 事件的本质是 父组件 向子组件传递事件对象，对象由事件名和回调函数构成。子组件会根据传入，维护自己的 $listener 对象，对各事件创建或 push 回调函数栈。</p>
<p>当子组件执行$emit，是在依次弹出栈内回调函数，并执行。是交由子组件管理的范畴，父组件无感知。是典型的观察者模式。</p>
<h2 id="5-sync的本质">5. .sync的本质</h2>
<pre><code>&lt;Component  :foo=&quot;val&quot; @update:foo=&quot;(payload)=&gt;{ val = payload }&quot;&gt;

&lt;!-- 实质是 语法糖 --&gt;
&lt;Component :foo.sync=&quot;val&quot;&gt;
</code></pre>
<pre><code>// 子组件使用
this.$emit('update:foo', payload)
</code></pre>
<p>实质是更方便地实现数据双向绑定。即数据流向父-&gt;子天然实现，子-&gt;父只需子组件emit相关事件即可，从而实现双向绑定。</p>
<p>那么掌握了以上知识了之后，我们如何实现跨越三层的一个双向绑定呢？</p>
<pre><code>&lt;!-- A Component --&gt;
&lt;template&gt;
    &lt;BComponent v-model=&quot;modalShow&quot;&gt;&lt;/BComponent&gt;
&lt;/template&gt;
 
&lt;script&gt;
    this.modalShow = true // 往下传递状态，直接到C
&lt;/script&gt;
 
 
&lt;!-- B Component --&gt;
&lt;template&gt;
    &lt;CComponent v-bind=&quot;$attrs&quot; v-on=&quot;$listeners&quot;&gt;&lt;/CComponent&gt;
&lt;/template&gt;
 
&lt;script&gt;
    // 中间组件也可以中途向两边更改状态,一致性需要手动保持
    // this.$attrs.value = true
    // this.$emit('input', true)
&lt;/script&gt;
 
 
&lt;!-- C Component --&gt;
&lt;script&gt;
    @Props()
    value:boolean
    
    handleClose(){
        this.$emit('input', false) // 往上emit状态，直接到A
    }
&lt;/script&gt;
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JS 闭包]]></title>
        <id>https://zouxiulong.github.io/post/bibao/</id>
        <link href="https://zouxiulong.github.io/post/bibao/">
        </link>
        <updated>2020-09-29T02:12:36.000Z</updated>
        <summary type="html"><![CDATA[<p>闭包（closure）是Javascript语言的一个难点，也是它的特色，很多高级应用都要依靠闭包实现。</p>
]]></summary>
        <content type="html"><![CDATA[<p>闭包（closure）是Javascript语言的一个难点，也是它的特色，很多高级应用都要依靠闭包实现。</p>
<!-- more --> 
<p>简单来说，闭包就是函数中的函数，就是说一个函数要访问另外一个目标函数内部的变量，就要在目标函数中再定义一个函数（以此来把作用域链往下延长一段，目的就是为了利用js在找自由变量时,会沿着作用域链一级一级往上找的特点），并将这个定义的函数return出来，供外部使用。</p>
<h2 id="javascript变量的作用域">Javascript变量的作用域</h2>
<p>有点类似于原型链（proto chain），Javascript中变量遵从作用域链（scope chain）规则。</p>
<figure data-type="image" tabindex="1"><img src="https://zouxiulong.github.io/post-images/1612752659326.webp" alt="" loading="lazy"></figure>
<p>如上图所示，在Javascript中，每一个函数体对应于一个作用域。当访问一个变量时，我们会先访问当前作用域内是否有定义该变量，如果没有就会在该作用域外的作用域内寻找是否有改变量，依此类推，一直寻找到全局变量。如果全局变量中依旧没有定义该变量，就会返回undefined。</p>
<p>我们来看下下面这个栗子：</p>
<pre><code>var a = '外面的特仑苏'
function foo1() {
    var a = '里面的特仑苏'
    console.log('我要喝' + a)   //我要喝里面的特仑苏
}
function foo2() {
    console.log('我要喝' + a)   //我要喝外面的特仑苏
}
foo1()
foo2()
</code></pre>
<p>在上述例子中，我们在 foo1 函数体内定义了变量a，因此 foo1 在寻找完当前作用域即可以得到里面的特仑苏，而在 foo2 函数体内没有定义变量a，它会沿着作用域链去寻找全局变量，然后得到了外面的特仑苏。</p>
</br>
<p>在函数外部无法读取函数内的局部变量。函数内部声明变量的时候，一定要使用var命令。如果不用的话，你实际上声明了一个全局变量！</p>
<pre><code>function foo3(){　　　　
  var a='里面的特仑苏';　　
}
alert(a); // error
</code></pre>
<pre><code>function foo4(){　　　　
  a='里面的特仑苏';　　
}
foo4();
alert(a); // 里面的特仑苏
</code></pre>
<h2 id="如何从外部读取局部变量">如何从外部读取局部变量？</h2>
<p>出于种种原因，我们有时候需要得到函数内的局部变量。但是，前面已经说过了，正常情况下，这是办不到的，只有通过变通方法才能实现。</p>
<p>那就是在函数的内部，再定义一个函数。</p>
<pre><code>function f1(){
　　a=999;
　　function f2(){
　　　　alert(a); // 999
　　}
}
</code></pre>
<p>在上面的代码中，函数 f2 就被包括在函数 f1 内部，这时 f1 内部的所有局部变量，对 f2 都是可见的。但是反过来就不行，f2 内部的局部变量，对 f1 就是不可见的。这就是Javascript语言特有的“链式作用域”结构（chain scope），</p>
<p>子对象会一级一级地向上寻找所有父对象的变量。所以，父对象的所有变量，对子对象都是可见的，反之则不成立。</p>
<p>既然 f2 可以读取 f1 中的局部变量，那么只要把 f2 作为返回值，我们不就可以在 f1 外部读取它的内部变量了吗！</p>
<pre><code>function f1(){
  a=999;
  function f2(){
　  alert(a);
  }
  return f2;
}
var foo=f1();
foo(); // 999
</code></pre>
<h2 id="闭包的概念">闭包的概念</h2>
<p>上一节代码中的f2函数，就是闭包。</p>
<p>各种专业文献上的“闭包”（closure）定义非常抽象，很难看懂。我的理解是，闭包就是能够读取其他函数内部变量的函数。</p>
<p>由于在Javascript语言中，只有函数内部的子函数才能读取局部变量，因此可以把闭包简单理解成“定义在一个函数内部的函数”。</p>
<p>所以，在本质上，闭包就是将函数内部和函数外部连接起来的一座桥梁。</p>
<h2 id="说到闭包就得提什么是gc垃圾回收机制">说到闭包就得提什么是GC（垃圾回收机制）</h2>
<p>简单来说：</p>
<ol>
<li>如果变量没有被引用JS会隔一段时间周期性的释放变量占用的内存空间。</li>
<li>全局变量不会被回收，只有浏览器卸载页面时才会结束。</li>
</ol>
<pre><code>function f1() {
    var x = 1
    var y = 100
    console.log(y)
    y++
    return function f2() {
        console.log(x++)
    }
}
var foo= f1()
// 100
foo() // 1
foo() // 2
</code></pre>
<p>当执行 foo() 时为什么输出x累加呢？y为什么不输出，这里的y没有被全局变量引用所以被JS垃圾回收销毁所以不会输出。<br>
多次执行 foo() 输出累加的x说明x并没有被销毁，因为 f2 函数被映射到了全局变量 foo 上，而 f2 函数又依赖x变量，所以 f1 函数中的x并不会被销毁。</p>
<h2 id="闭包应用">闭包应用：</h2>
<p>在实际开发中，闭包主要是用来封装变量实现公有私有变量，收敛权限。</p>
<p>优点：</p>
<ol>
<li>使变量的值始终保持在内存中。</li>
<li>避免全局变量的污染。</li>
<li>私有成员的存在。<br>
缺点：</li>
<li>因为不会被GC回收，所以常驻内存，会增大内存的使用量，使用不当会造成内存泄露。</li>
</ol>
</br>
<h2 id="使用闭包的注意点">使用闭包的注意点</h2>
<p>因为闭包会使得函数中的变量都被保存在内存中，内存消耗很大，所以不能滥用闭包，否则可能会造成内存泄露。解决方法是，在使用完闭包函数之后，将变量设置为null或者undefined。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JavaScript 中 call()、apply()、bind() 的用法]]></title>
        <id>https://zouxiulong.github.io/post/callapplybind/</id>
        <link href="https://zouxiulong.github.io/post/callapplybind/">
        </link>
        <updated>2020-09-18T09:07:44.000Z</updated>
        <summary type="html"><![CDATA[<p>在JavaScript中，bind、call和apply作用是一样的，都是为了改变某个函数运行时的上下文（context）而存在的，换句话说，就是为了改变函数体内部this的指向。</p>
]]></summary>
        <content type="html"><![CDATA[<p>在JavaScript中，bind、call和apply作用是一样的，都是为了改变某个函数运行时的上下文（context）而存在的，换句话说，就是为了改变函数体内部this的指向。</p>
<!-- more -->
<p>其实是一个很简单的东西，认真看十分钟就从一脸懵B 到完全 理解！<br>
先看明白下面例子：<br>
例1</p>
<pre><code>var name = '小邹',age = '18';
let obj={
		name: '小李',
		objAge: this.age,
		_fun: function(){
			console.log( this.name + '年龄' + this.age );
		}
}

obj.age;  // 25
obj._fun()  // 小李年龄 undefined
</code></pre>
<p>例2</p>
<pre><code>var name = '小李'
function _fun(){
		console.log( this.name );
}
_fun() // 小李
</code></pre>
<p>比较一下这两者 this 的差别，第一个打印里面的 this 指向 obj，第二个全局声明的 fun() 函数 this 是 window ；</p>
<h2 id="call-和-apply-的共同点">call 和 apply 的共同点</h2>
<p>它们的共同点是，都能够改变函数执行时的上下文，将一个对象的方法交给另一个对象来执行，并且是立即执行的。</p>
<p>为何要改变执行上下文？举一个生活中的小例子：平时没时间做饭的我，周末想给孩子炖个腌笃鲜尝尝。但是没有适合的锅，而我又不想出去买。所以就问邻居借了一个锅来用，这样既达到了目的，又节省了开支，一举两得。</p>
<p>改变执行上下文也是一样的，A 对象有一个方法，而 B 对象因为某种原因，也需要用到同样的方法，那么这时候我们是单独为 B 对象扩展一个方法呢，还是借用一下 A 对象的方法呢？当然是借用 A 对象的啦，既完成了需求，又减少了内存的占用。</p>
<p>另外，它们的写法也很类似，调用 call 和 apply 的对象，必须是一个函数 Function。</p>
<h2 id="call-和-apply-的区别">call 和 apply 的区别</h2>
<h3 id="call-的写法">call 的写法</h3>
<pre><code>Function.call(obj, param1, param2, ...paramN )
</code></pre>
<p>需要注意以下几点：</p>
<ul>
<li>调用 call 的对象，必须是个函数 Function。</li>
<li>call 的第一个参数，是一个对象。 Function 的调用者，将会指向这个对象。如果不传，则默认为全局对象 window。</li>
<li>第二个参数开始，可以接收任意个参数。每个参数会映射到相应位置的 Function 的参数上。但是如果将所有的参数作为数组传入，它们会作为一个整体映射到 Function 对应的第一个参数上，之后参数都为空。</li>
</ul>
<pre><code>function fun (a,b,c) {}

fun.call(obj, 1,2,3)
// fun 接收到的参数实际上是 1,2,3

fun.call(obj, [1,2,3])
// fun 接收到的参数实际上是 [1,2,3],undefined,undefined
</code></pre>
<h3 id="apply-的写法">apply 的写法</h3>
<pre><code>Function.apply(obj, Array)
</code></pre>
<p>需要注意的是：</p>
<ul>
<li>它的调用者必须是函数 Function，并且只接收<strong>两个参数</strong>，第一个参数的规则与 call 一致。</li>
<li>第二个参数，必须是<strong>数组或者类数组</strong>，它们会被转换成类数组，传入 Function 中，并且会被映射到 Function 对应的参数上。这也是 call 和 apply 之间，很重要的一个区别。</li>
</ul>
<pre><code>function fun (a,b,c) {}

fun.apply(obj, [1,2,3])
// func 接收到的参数实际上是 1,2,3

fun.apply(obj, {
    0: 1,
    1: 2,
    2: 3,
    length: 3
})
// fun 接收到的参数实际上是 1,2,3
</code></pre>
<h5 id="什么是类数组">什么是类数组？</h5>
<p>先说数组，这我们都熟悉。它的特征有：可以通过角标调用，如 array[0]；具有长度属性length；可以通过 for 循环或forEach方法，进行遍历。</p>
<p>那么，类数组是什么呢？顾名思义，就是具备与数组特征类似的对象。比如，下面的这个对象，就是一个类数组。</p>
<pre><code>var arrayLike = {
    0: 1,
    1: 2,
    2: 3,
    length: 3
};
</code></pre>
<p>类数组 arrayLike 可以通过角标进行调用，具有length属性，同时也可以通过 for 循环进行遍历。</p>
<p>类数组，还是比较常用的，只是我们平时可能没注意到。比如，我们获取 DOM 节点的方法，返回的就是一个类数组。再比如，在一个方法中使用 arguments 获取到的所有参数，也是一个类数组。</p>
<p>但是需要注意的是：<strong>类数组无法使用 forEach、splice、push 等数组原型链上的方法</strong>，毕竟它不是真正的数组。</p>
<h2 id="bind-的使用">bind 的使用</h2>
<p>最后来说说 bind。在 MDN 上的解释是：bind() 方法创建一个新的函数，在调用时设置 this 关键字为提供的值。并在调用新函数时，将给定参数列表作为原函数的参数序列的前若干项。</p>
<p>它的语法如下：</p>
<pre><code>Function.bind(thisArg, arg1, arg2, ...)
</code></pre>
<p>bind 方法 与 apply 和 call 比较类似，也能改变函数体内的 this 指向。不同的是，bind 方法的返回值是函数，并且需要稍后调用，才会执行。而 apply 和 call 则是立即调用。</p>
<p>来看下面这个例子：</p>
<pre><code>function add (a, b) {
    return a + b;
}

function sub (a, b) {
    return a - b;
}

add.bind(sub, 5, 3); // 这时，并不会返回 8
add.bind(sub, 5, 3)(); // 调用后，返回 8
</code></pre>
<p>如果 bind 的第一个参数是 null 或者 undefined，this 就指向全局对象 window。</p>
<h2 id="call-apply-bind-都是用来重定义-this-这个对象的">call()、apply()、bind() 都是用来重定义 this 这个对象的</h2>
<p>比如：</p>
<pre><code>let obj={
		name: '小李',
		objAge: this.age,
		_fun: function(){
				console.log( this.name + '年龄' + this.age );
		}
}
var db = {
		name: '小邹',
		age: '18'
}

obj._fun.call(db)；　　 // 小邹年龄 18
obj._fun.apply(db);　　// 小邹年龄 18
obj._fun.bind(db)();　   // 小邹年龄 18
</code></pre>
<p>call 和 apply 的主要作用，是改变对象的执行上下文，并且是立即执行的。它们在参数上的写法略有区别。</p>
<p>bind 也能改变对象的执行上下文，它与 call 和 apply 不同的是，返回值是一个函数，并且需要稍后再调用一下，才会执行。</p>
<h2 id="对比call-bind-apply-传参情况下">对比call 、bind 、 apply 传参情况下</h2>
<pre><code>let obj={
		name: '小李',
		objAge: this.age,
		_fun: function( f, t ){
				console.log( this.name + '年龄' + this.age, '来自' + f + '去往' + t );
		}
}
var db = {
		name: '小邹',
		age: '18'
}

obj._fun.call(db, '九江', '上海')；　　 // 小邹年龄 18  来自 九江去往上海
obj._fun.apply(db, ['九江', '上海']);　　// 小邹年龄 18  来自 九江去往上海
obj._fun.bind(db, '九江', '上海')();　   // 小邹年龄 18  来自 九江去往上海
obj._fun.bind(db, ['九江', '上海'])();　   // 小邹年龄 18  来自 九江, 上海去往 undefined
</code></pre>
<p>微妙的差距！</p>
<p>从上面四个结果不难看出:</p>
<p>call 、bind 、 apply 这三个函数的第一个参数都是 this 的指向对象，第二个参数差别就来了：</p>
<p>call 的参数是直接放进去的，第二第三第 n 个参数全都用逗号分隔，直接放到后面 <strong>obj._fun.call(db, '九江', ... ,'string' )</strong>。</p>
<p>apply 的所有参数都必须放在一个数组里面传进去 <strong>obj._fun.apply(db, ['九江', ..., 'string' ])</strong>。</p>
<p>bind 除了返回是函数以外，它 的参数和 call 一样。</p>
<p>当然，三者的参数不限定是 string 类型，允许是各种类型，包括函数 、 object 等等！</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[forEach、map、every、some和filter的用法]]></title>
        <id>https://zouxiulong.github.io/post/foreachmapeverysome-he-filter-de-yong-fa/</id>
        <link href="https://zouxiulong.github.io/post/foreachmapeverysome-he-filter-de-yong-fa/">
        </link>
        <updated>2020-08-14T06:26:51.000Z</updated>
        <summary type="html"><![CDATA[<p>在Javascript中，如何处理数组中的每一项数据？</p>
<p>有人可能会说，这还不简单，直接一个for循环遍历一下就好了。<br>
是的，确实，这是最常见的做法。<br>
但是，除此之外，ES5还提供了处理数组更加方便的方法，如题。<br>
接下来，我将通过几个简单的实例来具体讲解这几个方法。</p>
]]></summary>
        <content type="html"><![CDATA[<p>在Javascript中，如何处理数组中的每一项数据？</p>
<p>有人可能会说，这还不简单，直接一个for循环遍历一下就好了。<br>
是的，确实，这是最常见的做法。<br>
但是，除此之外，ES5还提供了处理数组更加方便的方法，如题。<br>
接下来，我将通过几个简单的实例来具体讲解这几个方法。</p>
<!-- more -->
<h3 id="1-foreach用于遍历数组无返回值">1. forEach，用于遍历数组，无返回值</h3>
<p>这里先给出一个数组（以下例子通用）：</p>
<pre><code>var arr = [1,-2,3,4,-5];
</code></pre>
<p>然后我要做事情的就是，将数组中的每一项翻倍。</p>
<pre><code>arr.forEach(function(item, index, array){
    array[index] = item * 2;
});
console.log(arr);   // [2,-4,6,8,-10]
</code></pre>
<p>可以看到，forEach 可以传入一个匿名函数作为参数，而该匿名函数有含有三个参数，其依次代表数组遍历时的当前元素item，数组遍历时的当前元素的索引index，以及正在遍历的数组array。有了这三个参数，可以方便我们做很多事情，比如说示例当中将每一项数组元素翻倍，这时需要用到第一个参数item。但是，仅仅只是将item乘以2可不行，我们还得将其赋值给原来的数组，这时我们就得用到后面两个参数index和array。<br>
根据上述可知，array[index]是全等于item的。</p>
<pre><code>arr.forEach(function(item, index, array){
    console.log(array[index] === item);   // true
});
</code></pre>
<h3 id="2-map用于遍历数组返回处理之后的新数组">2. map，用于遍历数组，返回处理之后的新数组</h3>
<pre><code>var newArr = arr.map(function(item, index, array){
    return item * 2;
});
console.log(newArr);   // [2,-4,6,8,-10]
</code></pre>
<p>可以看到，该方法与 forEach 的功能类似，只不过 map 具有返回值，会返回一个新的数组，这样处理数组后也不会影响到原有数组。</p>
<h3 id="3-every用于判断数组中的每一项元素是否都满足条件返回一个布尔值">3. every，用于判断数组中的每一项元素是否都满足条件，返回一个布尔值</h3>
<pre><code>var isEvery = arr.every(function(item, index, array){
    return item &gt; 0;
});
console.log(isEvery);   // false
</code></pre>
<p>可以看到，示例中是要判断数组arr中的元素是否都为正数，很显然不是，所以该方法最终返回false。</p>
<h3 id="4-some用于判断数组中的是否存在满足条件的元素返回一个布尔值">4. some，用于判断数组中的是否存在满足条件的元素，返回一个布尔值</h3>
<pre><code>var isSome = arr.some(function(item, index, array){
    return item &lt; 0;
});
console.log(isSome);   // true
</code></pre>
<p>可以看到，该方法与 every 类似，示例中是要判断数组arr中是否存在负数元素，很显然存在，所以该方法最终返回true。</p>
<h3 id="5-filter用于筛选数组中满足条件的元素返回一个筛选后的新数组">5. filter，用于筛选数组中满足条件的元素，返回一个筛选后的新数组</h3>
<pre><code>var minus = arr.filter(function(item, index, array){
    return item &lt; 0;
});
console.log(minus);   // [-2, -5]
</code></pre>
<p>可以看到，示例中是要筛选出数组arr中的所有负数，所以该方法最终返回一个筛选后的新数组[-2, -5]。</p>
<h2 id="补充">补充</h2>
<p>以上五大方法除了传递一个匿名函数作为参数之外，还可以传第二个参数，该参数用于指定匿名函数内的this指向，例如：</p>
<pre><code>// 只传一个匿名函数
arr.forEach(function(item, index, array){
    console.log(this);  // window
});
// 传两个参数
arr.forEach(function(item, index, array){
    console.log(this);  // [1, -2, 3, 4, -5]
},arr);
</code></pre>
<h2 id="重点总结">重点总结</h2>
<ol>
<li>forEach 无返回值，map 和 filter 返回新数组，every 和 some 返回布尔值</li>
<li>匿名函数中this指向默认为window，可通过传第二参数来更改之</li>
<li>五种遍历方法均为ES5方法</li>
</ol>
]]></content>
    </entry>
</feed>