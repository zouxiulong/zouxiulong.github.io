<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zouxiulong.github.io</id>
    <title>邹秀龙的博客</title>
    <updated>2021-01-13T02:13:57.848Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zouxiulong.github.io"/>
    <link rel="self" href="https://zouxiulong.github.io/atom.xml"/>
    <subtitle>咸鱼一枚~</subtitle>
    <logo>https://zouxiulong.github.io/images/avatar.png</logo>
    <icon>https://zouxiulong.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, 邹秀龙的博客</rights>
    <entry>
        <title type="html"><![CDATA[git总结]]></title>
        <id>https://zouxiulong.github.io/post/git</id>
        <link href="https://zouxiulong.github.io/post/git">
        </link>
        <updated>2020-07-01T01:43:47.000Z</updated>
        <summary type="html"><![CDATA[<p>每次git都在百度，终于这次整理了一下自己当前所学的git知识。这篇文章只是我的个人总结方便记忆用的，并不适合初学者来学习git。</p>
]]></summary>
        <content type="html"><![CDATA[<p>每次git都在百度，终于这次整理了一下自己当前所学的git知识。这篇文章只是我的个人总结方便记忆用的，并不适合初学者来学习git。</p>
<!-- more -->
<h3 id="1-创建版本库">1. 创建版本库</h3>
<p>执行 git init ，把当前目录变成Git可以管理的仓库，可以发现当前目录下多了一个.git的目录，个目录是Git来跟踪管理版本库的，没事千万不要手动修改这个目录里面的文件，不然改乱了，就把Git仓库给破坏了。</p>
<p>如果你没有看到.git目录，那是因为这个目录默认是隐藏的，用 ls -ah 或 ls -la 命令就可以看见。</p>
<h3 id="2-添加文件到git仓库">2. 添加文件到Git仓库</h3>
<ul>
<li>第一步，使用命令 git add <file> ，注意，可反复多次使用，添加多个文件；</li>
<li>第二步，使用命令 git commit -m &quot;本次提交说明&quot; ,完成。
例如：</li>
</ul>
<pre><code class="language-text">$ git add file1.txt
$ git add file2.txt file3.txt
$ git commit -m &quot;add 3 files&quot;
</code></pre>
<h3 id="3-添加文件到git仓库">3. 添加文件到Git仓库</h3>
<ul>
<li>要随时掌握工作区的状态，使用git status命令。</li>
<li>如果git status告诉你有文件被修改过，用git diff可以查看修改内容。</li>
</ul>
<h3 id="4-版本回退">4. 版本回退</h3>
<ul>
<li>可以通过 git log 查看提交版本记录。</li>
<li>在Git中，用HEAD表示当前版本，上一个版本就是HEAD^ ，上上一个版本就是HEAD^^</li>
<li>要把当前版本回退到上一版本以使用git reset命令。</li>
</ul>
<pre><code class="language-text">$ git reset --hard HEAD^
</code></pre>
<ul>
<li>回到未来的某个版本</li>
</ul>
<pre><code class="language-text">$ git reset --hard 329782f  //其中329782f是版本的commit id
</code></pre>
<ul>
<li>Git提供了一个命令git reflog用来记录你的每一次命令</li>
</ul>
<h4 id="小结">小结：</h4>
<ul>
<li>HEAD指向的版本就是当前版本，因此，Git允许我们在版本的历史之间穿梭，使用命令git reset --hard commit_id。</li>
<li>穿梭前，用git log可以查看提交历史，以便确定要回退到哪个版本。</li>
<li>要重返未来，用git reflog查看命令历史，以便确定要回到未来的哪个版本。</li>
</ul>
<h3 id="5-工作区和暂存区">5. 工作区和暂存区</h3>
<ul>
<li>
<h4 id="工作区working-directory">工作区（Working Directory）</h4>
</li>
</ul>
<p>就是你在电脑里能看到的目录，比如我的learngit文件夹就是一个工作区</p>
<ul>
<li>
<h4 id="版本库repository">版本库（Repository）</h4>
</li>
</ul>
<p>工作区有一个隐藏目录.git，这个不算工作区，而是Git的版本库。</p>
<p>Git的版本库里存了很多东西，其中最重要的就是称为stage（或者叫index）的暂存区，还有Git为我们自动创建的第一个分支master，以及指向master的一个指针叫HEAD。</p>
<p><img src="https://zouxiulong.github.io/post-images/1593570454964.webp" alt=""></p>
<p>前面讲了我们把文件往Git版本库里添加的时候，是分两步执行的：</p>
<ul>
<li>第一步是用git add把文件添加进去，实际上就是把文件修改添加到暂存区；</li>
<li>第二步是用git commit提交更改，实际上就是把暂存区的所有内容提交到当前分支。</li>
<li>因为我们创建Git版本库时，Git自动为我们创建了唯一一个master分支，所以，现在git commit就是往master分支上提交更改。</li>
<li>你可以简单理解为，需要提交的文件修改通通放到暂存区，然后一次性提交暂存区的所有修改。</li>
</ul>
<h3 id="6-撤销修改">6. 撤销修改</h3>
<ul>
<li>
<p>场景1：当你改乱了工作区某个文件的内容，想直接丢弃工作区的修改时，用命令git checkout -- file。</p>
</li>
<li>
<p>场景2：当你不但改乱了工作区某个文件的内容，还添加到了暂存区时，想丢弃修改，分两步，第一步用命令git reset HEAD file，就回到了场景1，第二步按场景1操作。</p>
</li>
<li>
<p>场景3：已经提交了不合适的修改到版本库时，想要撤销本次提交，参考版本回退一节，不过前提是没有推送到远程库。</p>
</li>
</ul>
<h3 id="7-删除文件">7. 删除文件</h3>
<p>从版本库中删除该文件，那就用命令git rm删掉，并且git commit
git checkout其实是用版本库里的版本替换工作区的版本，无论工作区是修改还是删除，都可以“一键还原”。</p>
<h3 id="8-分支管理">8. 分支管理</h3>
<ul>
<li>查看分支：git branch</li>
<li>查看远程分支 git branch -r</li>
<li>创建分支：git branch <name></li>
<li>切换分支：git checkout <name></li>
<li>创建+切换分支：git checkout -b <name></li>
<li>合并某分支到当前分支：git merge <name></li>
<li>删除分支：git branch -d <name></li>
<li>查看远程库信息，使用git remote -v；</li>
</ul>
<h3 id="9-多人协作">9. 多人协作</h3>
<p>本地新建的分支如果不推送到远程，对其他人就是不可见的；
从本地推送分支，使用git push origin branch-name，如果推送失败，先用git pull抓取远程的新提交；
在本地创建和远程分支对应的分支，使用git checkout -b branch-name origin/branch-name，本地和远程分支的名称最好一致；
建立本地分支和远程分支的关联，使用git branch --set-upstream branch-name origin/branch-name；
从远程抓取分支，使用git pull，如果有冲突，要先处理冲突。</p>
<h3 id="10-工作中实践补充">10. 工作中实践补充</h3>
<pre><code class="language-text">//远程分支版本回退
	
如果你的错误提交已经推送到自己的远程分支了，那么就需要回滚远程分支了。 
首先要回退本地分支：
git reflog 查出git提交记录，然后找到需要回滚到的commitId，执行回滚命令：
git reset --hard commitId
紧接着强制推送到远程分支：
git push -f
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Hello World~]]></title>
        <id>https://zouxiulong.github.io/post/hello-world</id>
        <link href="https://zouxiulong.github.io/post/hello-world">
        </link>
        <updated>2020-06-30T08:41:16.000Z</updated>
        <summary type="html"><![CDATA[<p>👏  欢迎 ！<br>
✍️  这是一个静态测试页面。可以用它来记录你的生活、心情、知识、笔记、创意......</p>
]]></summary>
        <content type="html"><![CDATA[<p>👏  欢迎 ！<br>
✍️  这是一个静态测试页面。可以用它来记录你的生活、心情、知识、笔记、创意......</p>
<!-- more -->
<p>个人<a href="https://github.com/zouxiulong">Github</a></p>
<h2 id="现在暂时还没什么好说的~~">现在暂时还没什么好说的~~</h2>
<p>😘 Enjoy~</p>
]]></content>
    </entry>
</feed>